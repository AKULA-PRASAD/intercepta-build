# INTERCEPTA build ledger — verified / falsified / untestable (tiered, never blurred)

Carried forward from the audits in `~/kaalcura` (HARD_TEST_LEDGER, COMPRESSED_THEORY_AND_LEDGER) and
`~/INTERCEPTA/verification` (INTERCEPTA_VERIFICATION_LEDGER). Only entries reproduced ×2 with a committed
metrics file are marked VERIFIED. Each row states the killing/confirming test.

## VERIFIED (reproduced ×2, controlled, provenance on file)
| # | Finding | Evidence | Source metrics |
|---|---|---|---|
| V1 | Learned expression→response map transfers cross-dataset, beats parameter-free bar | STRICT (disjoint cell lines) mean per-drug ρ=+0.212, 94/100 drugs>0, Wilcoxon p=1.9e-15 vs R_prolif +0.058 | v1b_learned_metrics.json |
| V2 | R_prolif is a real proliferation signal but ≈ GGI (not novel) | r(R_prolif,MKI67)=0.43; r≈0.75 vs GGI; prognostic for chemo pCR | deconfound_metrics.json, compression_metrics.json |
| V3 | Two-axis (prolif+immune) prognostic pCR model | I-SPY2 GSE173839 AUROC 0.795; R_immune adds p=0.019, permutation p=0.0005 | ispy2_harden_metrics.json |
| V4 | NPM1 → Cabozantinib sensitivity (AML) | BeatAML MW p=4.4e-11; ITD-adjusted OLS β=−21.9 p=3.3e-5; split-replicates both halves | beataml_npm1_cabo_metrics.json, ..._split_replication.json |
| V5 | NRAS → MEK-inhibitor sensitivity (AML) | 3 drugs consistent, p~1e-9 to 3e-11 (known biology) | beataml_other_claims_metrics.json |
| V6 | DNMT3A → Dasatinib sensitivity (AML), independent of NPM1/FLT3-ITD | MW p=1.6e-5; OLS β=−23.7 p=1.2e-4 | beataml_other_claims_metrics.json |
| V7 | +0.212 is the public cell-line cross-dataset ceiling (well-powered null, B2) | Adding R_prolif Δρ=+0.0000 p=0.98; adding 50 driver mutations Δρ=+0.0004 p=0.37 BHq=0.74 — neither verified signal beats transcriptome-only | B2_metrics.json |
| V8 | Cell-line→PATIENT transfer is REAL but (on mismatched platform) non-specific (B3, L1) | GDSC array map → BeatAML patient ex-vivo AUC, 44 drugs: diagonal ρ=+0.054 permutation p=0.0005 (transfer real); diag−offdiag=+0.022 p=0.12 (not specific on array platform) | B3_metrics.json |
| V9 | On a MATCHED RNA-seq platform, a WEAK but DRUG-SPECIFIC, proliferation-independent cell-line→patient signal exists AND replicates across two independent screens (B3b/L1b + B3c) | DepMap RNA-seq map → BeatAML: prolif-residualized diag−off = +0.040 perm p=0.010 (GDSC2 labels, 44 drugs); replicates with independent GDSC1 labels: +0.051 perm p=0.0015 (59 drugs). Effect is small (diag ρ≈0.07–0.08). **Still single patient cohort (BeatAML, AML) — needs a 2nd patient cohort + other cancers for full external validity.** | B3b_metrics.json, B3c_metrics.json |
| V9+ | V9 is ROBUST to drug- and patient-subsetting (B3d) | Drug jackknife all leave-one-out >0 (min +0.033); bootstrap 95% CI over drugs [+0.008,+0.074] excludes 0; internal patient split-half both halves >0 (+0.053/+0.027). Not driven by any single drug or patient subset. | B3d_metrics.json |
| N1 | The robust transfer signal is NOT explained by AML mechanism (pre-registered NULL, B3e) | H1: drugs in GDSC pathways {RTK signaling, ERK MAPK signaling} (frozen a priori as the AML driver-signaling axis) do NOT transfer better — median resid ρ +0.0519 vs +0.0513, gap +0.0005, one-sided MWU p=0.29, perm p=0.50. H2: transfer ρ does not track within-cell-line CV predictability (Spearman +0.042, p=0.36). B3d's exploratory "AML-relevant drugs transfer best" ranking did NOT survive a rigorous pre-registered test — the visually-striking top drugs were selection, not signal. **Coherence claim withdrawn.** | B3e_metrics.json |
| V10 | The two verified signal types are COMPLEMENTARY — combining mutation marker + expression transfer beats either alone (B4, pre-registered) | On verified drug–marker pairs: combined 5-fold CV Spearman > BOTH single predictors in all 4 testable pairs (trametinib 0.336 vs mk 0.078/tr 0.257; selumetinib 0.380 vs 0.177/0.288; dasatinib 0.213 vs 0.050/0.180; sorafenib 0.469 vs 0.271/0.383). Expression transfer adds beyond the marker (partial p<0.05, correct sign, BHq<5e-8) in 3/4 pairs; complementary (both add) in 3/4. Pre-registered rule (≥3 pairs) MET. **Caveats (honest):** (a) DL random-effects meta is NULL (p=0.21) due to heterogeneity; (b) dasatinib's transfer is INVERTED (β<0, strongly anti-predictive — cell-line dasatinib biology ≠ patient); (c) cabozantinib/NPM1 (V4) NOT testable — no GDSC2 cabozantinib training data; (d) 4 pairs, single cohort (BeatAML), needs an INDEPENDENT cohort. | B4_metrics.json |
| V11 | engine v1 (shipped) faithfully embodies V10, using genome-wide-robust markers | Markers now sourced from B5 (genome-wide screen). Combining marker+transfer beats transfer-alone at predicting BeatAML ex-vivo response in 3/3 genome-wide-robust pairs (sorafenib ρ=−0.47 vs −0.39; selumetinib −0.31 vs −0.25; trametinib −0.30 vs −0.24); reproduced ×2. LOW confidence, one cohort, most correlation is generic proliferation (V8/N1). | engine_v1_metrics.json |
| V12 | Genome-wide mutation→drug marker screen (B5): 177 robust markers | 3051 gene×drug pairs in BeatAML; 177 survive BH-FDR<0.05 + FLT3-ITD/prolif-deconfounding + split-half direction-replication. Dominated by **FLT3-ITD→FLT3 inhibitors** (66; sorafenib BHq=4e-26) and **RAS(KRAS/NRAS)→MEK** — textbook actionable AML biology. 114 sensitizing / 63 resistance (incl. TP53→resistance). ONE cohort — needs external replication. | B5_metrics.json, discovered_markers.json |
| V13 | Engine confidence: OOD distance is a VALIDATED accuracy gate; per-drug reliability is NOT (B6) | H2: BeatAML samples closer to the cell-line training distribution (low OOD) transfer +0.051 more accurately than far samples (perm p=0.0055). H1: cell-line CV reliability does NOT predict patient accuracy (Spearman +0.02, p=0.45 — confirms B3e). Engine now gates confidence by OOD (MODERATE if low-OOD else LOW; capped — absolute accuracy weak); reliability kept descriptive-only. Reproduced ×2. | B6_metrics.json |
| V14 (DOWNGRADED) | PDXE external drug-specificity is BORDERLINE and NOT ROBUST | B7 (GDSC-trained, 9 drugs): prolif-resid diag>off p=0.036. B9 POWERED replication (PRISM-trained, 11 drugs): p=0.076 — does NOT hold. Raw transfer magnitude NS in both (B7 p=0.14, B9 p=0.31); R_prolif-only transfer (+0.087) EXCEEDS the drug-specific diagonal (+0.023). HONEST CORRECTION: the previous-turn 'first external replication' was borderline and is NOT robust to training-source/drug-set — external drug-specificity in PDXE is UNESTABLISHED. What DOES transfer to PDXE is generic proliferation. | B7_metrics.json, B9_metrics.json |
| V15 | FUNCTIONAL gene-dependency predicts drug response AND beats baseline expression (B12, cell lines) | Target-gene CRISPR dependency vs target-drug sensitivity across 21 established pairs: pooled ρ=+0.187 (perm p=5e-4); strong per-pair — MDM2→idasanutlin +0.475 (BHq=3e-23), EGFR +0.34-0.39, PIK3CA +0.35, CDK6/MEK/BRAF sig. Head-to-head: median \|ρ\| dependency 0.134 > expression 0.071 (perm p=0.015); for PIK3CA/MEK/MDM2 baseline expression is ~0/wrong-signed. **Validates the thesis: functional readouts carry far more drug-response signal than baseline transcriptomics** (dependency ρ up to +0.47 vs transfer +0.05-0.07). SCOPE: cell-line only (dependency unmeasurable in patients) → strengthens MECHANISM + reorients engine to functional features; NOT itself a patient predictor. | B12_metrics.json |
| V16 | Expression-inferred dependency is a learnable, patient-translatable functional layer (B13) | expr→CRISPR-dependency IS learnable (CV ρ: MDM2 0.58, EGFR 0.56, CDK6 0.59, PIK3CA 0.46, MEK 0.27). Inferred-dependency→drug beats single-gene baseline expression (median \|ρ\| 0.28 vs 0.07, perm p=0.010) and recovers ~100%+ of the TRUE-dependency signal (for MEK exceeds single-gene true dep: +0.36 vs +0.20 — transcriptome captures pathway state). Needs ONLY expression at inference → patient-translatable in principle. **CAVEATS:** cell-line (patient translation is a Track-1 hypothesis, unproven); target-anchored/oncogene-addicted drugs; the FAIR decisive comparison vs the FULL-transcriptome direct model is pending (B14) — so 'beats baseline' = beats single-gene expression, not yet proven to beat all expression models. | B13_metrics.json |
| V17 | Functional-inference layer RESCUES prediction for actionable AML targets where direct transfer fails (B14, BeatAML ex-vivo) | expr→inferred-dependency (trained on DepMap, applied to BeatAML patient RNA) predicts ex-vivo response: pooled prolif-resid ρ=+0.055 (perm p=5e-4). NOT globally better than direct transfer (H2 median 0.116 vs 0.093, p=0.27). BUT the PRE-DECLARED key case is decisive: for **FLT3 inhibitors** (sorafenib +0.24, crenolanib +0.16, quizartinib +0.14, gilteritinib +0.13) and **venetoclax→BCL2 (+0.22)** — the most actionable AML targets — inferred-dependency STRONGLY predicts sensitivity while direct transfer is ~0 or WRONG-SIGNED (sorafenib +0.04, crenolanib −0.14, quizartinib −0.11). MEK: direct wins (RAS-driven); EGFR: neither (AML not EGFR-driven) — all mechanistically coherent. **The functional layer rescues exactly the actionable, transcriptome-direct-failing targets, from patient expression alone.** SCOPE: BeatAML ex-vivo (not clinical outcome); weak overall; the clearest translational lead → Track-1. | B14_metrics.json |
| V18 | Functional-inference rescue is SPECIFIC to dependency-driven actionable targets (B15, 26 drugs) | NOT a broad advantage over direct transfer (H1 median 0.086 vs 0.079, p=0.40). BUT 9/26 drugs rescued (inferred-dep sig BH<0.05 & beats direct), clustering into a coherent, clinically-central set: **FLT3** (sorafenib +0.24, dovitinib +0.23, crenolanib +0.16, quizartinib +0.14, gilteritinib +0.13 — direct ~0/wrong-signed), **BCL2** (venetoclax +0.22), **CDK9** (flavopiridol +0.20, sns-032 +0.14), **AURKA** (alisertib +0.14). NOT rescued: MEK (direct wins, RAS-driven), EGFR (AML not EGFR-driven), MET/SYK/JAK/KDR/XPO1 — mechanistically coherent. **The functional layer specifically rescues dependency/oncogene-addiction targets (FLT3+BCL2 = the pillars of targeted AML therapy) from patient RNA where direct transcriptomics fails.** Scope: BeatAML ex-vivo, pan-cancer-trained dep. | B15_metrics.json |
| V19 ⚠ | **DOWNGRADED — does NOT externally replicate (B20, FIMM). Internally verified in BeatAML only.** Inferred-FLT3-dependency predicts FLT3-inhibitor response BEYOND FLT3-ITD, incl. in ITD-WILDTYPE patients (B16, BeatAML ex-vivo) | RNA→inferred-FLT3-dependency (engine functional layer) adds over the standard clinical biomarker: OLS AUC~dep+FLT3-ITD+prolif, meta β=+7.57 p=8.2e-11 across 4 FLT3 inhibitors (sorafenib 1.3e-5, crenolanib 1.1e-3, quizartinib 3.2e-3). **Within FLT3-ITD-WT patients it still predicts FLT3i sensitivity: pooled ρ=+0.219, p=1.5e-15** (n_wt ~350/drug) — identifies FLT3i-sensitive AML the mutation test MISSES. ROBUSTNESS: the ITD-WT signal SURVIVES proliferation adjustment (prolif-resid: sorafenib +0.295, crenolanib +0.231, quizartinib +0.156, gilteritinib +0.104; combined Stouffer p=1.85e-13) — genuine FLT3-dependency, independent of both mutation AND proliferation (B16_robustness_itdwt_prolif.json). Novel, mechanistically coherent (FLT3-dependence ⊋ FLT3-ITD), well-powered, reproduced ×2. **SCOPE (honest):** BeatAML EX-VIVO (not clinical outcome — ex-vivo→clinic is the Track-1 test); FLT3 only; dep trained on pan-cancer DepMap. A strong translational lead, NOT a validated clinical test yet. **CROSS-LINEAGE ROBUSTNESS (B19, falsifies the memorization critique):** retraining the dependency model with ALL 25 DepMap AML lines REMOVED (model never sees AML) leaves V19 essentially unchanged — beyond-ITD meta β=+7.41 p=1.9e-10, ITD-WT ρ=+0.230 p=4.8e-17 (vs full-DepMap control β=+7.57, ρ=+0.219). So inferred-FLT3-dependency is NOT re-detecting AML lineage — it is genuine cross-lineage functional transfer. (Excluding ALL blood/lymphoid lines drops H1 to null, β=+1.39 p=0.41, but this is the biology of FLT3-dependence being hematopoietic — only 2 FLT3-dependent solid anchors remain — not a failure of V19; H2 residual ρ=+0.079 p=2e-3 still holds.) Reproduced ×2. | B16_metrics.json, B19_metrics.json |
| V20 ⚠ | **DOWNGRADED — does NOT externally replicate (B20, FIMM); BeatAML only.** The inferred-dependency layer is TARGET-SPECIFIC (reads WHICH vulnerability, not generic sensitivity) — double dissociation FLT3 vs BCL2 (B18, BeatAML ex-vivo) | Proliferation-adjusted Spearman matrix (2 inferred dependencies × 5 drugs, n≥367/cell): matched (diagonal) ρ=+0.193 vs mismatched (off-diagonal) ρ=+0.068, **gap +0.125, target↔drug-shuffle permutation p<1e-4** (H1). **FLT3 side is a clean double dissociation** (H3): FLT3-dep→FLT3 inhibitors pooled ρ=+0.196 (Stouffer p=2e-16) but FLT3-dep→venetoclax ρ=**−0.03** (null). Venetoclax is predicted **only** by BCL2-dep (ρ=+0.205, p=8e-5), not FLT3-dep — so venetoclax response is BCL2-specific from the drug side. **HONEST NUANCE (H2 partial):** BCL2-dep also predicts FLT3 inhibitors (pooled ρ=+0.10, p=2e-5) — i.e. BCL2-dependence is a *broader* apoptotic-priming feature than FLT3-dependence, biologically expected (FLT3-ITD AML is BCL2-dependent/venetoclax-sensitive; the basis for venetoclax+FLT3i combinations). **Bottom line: the functional layer is genuinely target-specific (V19 generalizes to a second, mechanistically-independent pillar), with FLT3-dependence the more restricted signal and BCL2-dependence the broader one — not an artifact.** Reproduced ×2. SCOPE: BeatAML ex-vivo; venetoclax is a single BCL2 drug. | B18_metrics.json |
| V21 | The cell-line drug-response ceiling is MODALITY-GENERAL, not RNA-specific (B22) — proteomics does not break it | Fair head-to-head on 291 cell lines with matched CCLE proteomics (Nusinow 2020) + DepMap RNA + GDSC2 response, identical 5-fold CV, 271 drugs: mean per-drug ρ **RNA +0.419 > protein +0.328** (paired Wilcoxon p=1e-43; protein wins in only 5.9% of drugs) and **RNA+protein +0.408 ≤ RNA alone** (adding proteomics does NOT help, p=2e-9). Pre-registered H2 (proteomics adds drug-specificity RNA lacks) FALSIFIED. **Interpretation:** no baseline molecular profile (RNA or protein) resolves within-lineage drug specificity beyond the ceiling here — the limit is intrinsic to baseline profiling, strengthening V7 and the functional/perturbation (Track-1) argument. HONEST BOUND: protein underperformance is partly measurement coverage/noise (7,947 proteins, 27% missing, vs 19,177 genes), so the defensible claim is "proteomics does not break or add beyond the RNA ceiling," not "protein biology is uninformative." Reproduced ×2. | B22_metrics.json |
| V22 | Even MEASURED functional dependency does not break the baseline ceiling; the functional advantage is TARGET-TAUTOLOGICAL (B23) — the unifying explanation for B20 | Head-to-head on 498 cell lines (matched DepMap RNA + CRISPR gene-effect + GDSC2), identical 5-fold CV, 272 drugs: mean per-drug ρ **RNA +0.487 ≥ measured-dependency +0.459** (dependency alone is slightly WORSE; paired p=3e-16, dep wins in 27% of drugs). Integrating RNA+dependency adds only **+0.019** (p=5e-18, improves 72% of drugs) — real and robust but **below the pre-registered +0.02 materiality bar** for "ceiling broken" (H2 not met; goalpost not moved). **Target-leakage control (26 curated single-target drugs):** with each drug's own target-gene dependency EXCLUDED, dependency (ρ=0.504) does NOT beat RNA (0.514, Wilcoxon p=0.06) — so the functional advantage that exists is **target-specific/tautological**, not a generalizable functional-state signal (H3 fails). **Unifying conclusion:** the real functional signal (V15) is a drug's own target dependency; there is NO transferable functional-state predictor beyond baseline + the direct target — which is exactly why the *inferred* functional layer failed external replication (B20/B21). The ceiling is intrinsic to baseline profiling AND holds for measured genome-wide function. Reproduced ×2. HONEST BOUND: cell-line internal CV; dependency has its own noise/missingness; curated target map is representative not exhaustive. | B23_metrics.json |
| V23 (refined by B25) | Drug-combination SYNERGY generalizes to unseen combinations of KNOWN drugs — robust across TWO independent corpora; novel-drug generalization does NOT replicate | **B24 (O'Neil, 583 pairs×39 lines):** leave-drug-COMBINATION-out Spearman **+0.606** vs drug-marginal baseline +0.474 (Δ+0.132, CI95 [0.124,0.140], p≈0), synergy AUROC 0.804. **B25 (DrugComb, 5,416 pairs×41 lines×124 drugs, ×2):** leave-combination-out Spearman **+0.385** vs baseline +0.290 (Δ+0.094) — the combination-generalization signal REPLICATES on an independent, more diverse, noisier corpus. **DOWNGRADED — novel-drug generalization does NOT hold:** B24 leave-DRUG-out (novel chemistry) was +0.246, but on DrugComb's 124-drug set it **collapses to +0.025 (CI95 [0.012,0.038])** — i.e. the 0.25 was inflated by O'Neil's small, chemically-redundant, single-study drug set; true out-of-chemistry synergy prediction does NOT generalize (B25 H1 falsified). **Net honest claim:** synergy is predictable & generalizes for **new pairings within a KNOWN drug library** (the realistic use case; robust on both corpora, NOT bound by the +0.212 single-agent ceiling), but **NOT for novel drugs**. First real generalizing signal in the program, now honestly bounded. Reproduced ×2 (both B24, B25). **SCOPE:** cell-line Loewe synergy (not clinical); standard benchmark+model (consistent with field, not SOTA). **SHIPPED as a usable module** `intercepta.synergy.SynergyRanker` (rank synergistic pairs from a known library for a query expression profile, absolute-OOD-gated, confidence capped at moderate). **Two libraries:** O'Neil (38 drugs, 703 pairs, self-validated leave-combination-out CV Spearman **0.62**) and DrugComb (124 drugs, **7,626 rankable pairs**, CV Spearman **0.39** — ~3× more drugs but lower per-prediction reliability, matching B25's reproduced 0.385; honest tradeoff). CLI `intercepta synergy --library {oneil,drugcomb}`. Both cell-line Loewe, known-drug-library only, not clinical. **EXTERNALLY VALIDATED across independent corpora (B28, hardening, reproduced ×2):** a synergy model transfers between O'Neil and DrugComb (different institution + Loewe computation). Asymmetric and honest — training on the **diverse** corpus generalizes strongly: **DrugComb→O'Neil Spearman +0.38** (CI [0.36,0.39]), retrieval **precision@10% 0.62 vs 0.25 base = 2.5× enrichment**, and it **holds on genuinely novel combinations** (ρ=+0.44); the reverse (small O'Neil→diverse DrugComb) is weak (ρ=+0.086, 1.36×) because 38 drugs can't cover DrugComb's diversity and its labels are noisier. Both directions positive with CI>0 (H1+H3 pass). The synergy signal is NOT corpus-specific — it externally replicates when trained on diverse data, with practically-useful retrieval. **CALIBRATED UNCERTAINTY (B29, reproduced ×2):** the shipped tool emits conformal prediction intervals calibrated on leave-combination-out residuals; empirical coverage matches nominal on unseen combinations — O'Neil 90%→89.8%, 80%→79.5%; DrugComb 90%→90.5%, 80%→80.6% (within ±0.6%). Intervals are WIDE (90% ≈ ±20–24 Loewe units) — the honest signal that individual point predictions carry substantial uncertainty. | B24_metrics.json, B25_metrics.json, B28_metrics.json, B29_metrics.json, synergy_module_validation.json |
| B26 (negative, first-class) | Mechanism-anchored synergy (target-dependency features) does NOT beat chemical fingerprints for novel drugs — the co-dependency hypothesis is falsified as a *transferable* synergy signal | Novel pre-registered hypothesis (from V22+B25): encode each drug by its TARGET's CRISPR gene-dependency (biology-grounded, defined for unseen drugs, no synergy-label leakage) and test whether it generalizes to NOVEL drugs where fingerprints collapsed. O'Neil, 31 targeted drugs, 32 DepMap-CRISPR-mapped cells, curated drug→target map. **Leave-DRUG-out Spearman: MECH +0.076 < FP +0.162 (delta p=0.001) — H_mech FALSIFIED** (mechanism is WORSE, not better, for novel drugs). Honest nuances: target-dependency DOES carry real synergy signal (MECH leave-combination-out +0.458 ≫ cell-only +0.130, H_sanity pass), and FP+MECH marginally beats FP (combos 0.642 vs 0.629; drug-out 0.174 vs 0.162, H_add pass) — but the increment is tiny and the core novel claim fails. **Root cause:** synergy is driven by more than target co-dependency; the low-dim target-dependency encoding is a weak proxy vs chemical structure. **Net:** the elegant "synergy = co-dependency" story is real but weak, NOT the dominant driver, and does not rescue novel-drug generalization. V23 (known-drug-combination generalization) stands unchanged. Reproduced ×2. | B26_metrics.json |
| B27 (negative, first-class) | LINCS connectivity-based repurposing: signature-reversal predicts efficacy at a STATISTICALLY-SIGNIFICANT but PRACTICALLY-NEGLIGIBLE level — not a useful signal | Open LINCS L1000 consensus signatures (dhimmel/lincs Zenodo 47223; 239 drugs ∩ PRISM, 7,103 shared genes) vs PRISM efficacy across 476 cell lines. Pre-registered within-cell test (does signature-reversal rank drugs by sensitivity?): mean within-cell Spearman(reversal, sensitivity) = **+0.021**, permutation p=5e-4 — significant ONLY because of huge N (476 cells × ~212 drugs); **|ρ|=0.02 explains ~0.04% of variance → practically useless.** Proliferation-robust (0.0205; a genuine-but-vanishing effect, not an artifact). **Honest conclusion (judged on effect size, not p-value):** connectivity-based repurposing does NOT provide a usable efficacy signal here — consistent with the program theme that generic transcriptomic signals are real-but-negligible. Reproduced ×2. | B27_metrics.json |
| B30 (module, validated ×2 — NOT a SOTA/clinical claim) | A structure-only ADMET/safety predictor is a genuine, honestly-benchmarked *screening filter* — pipeline module #4 (Absorption/Distribution/Metabolism/Excretion/Toxicity from SMILES). Beats the trivial baseline on **22/22** TDC ADMET tasks; lands mid-leaderboard, trailing graph/foundation models as expected. | TDC ADMET Benchmark Group (22 tasks, OPEN, scaffold splits + public leaderboard), official 5-seed protocol, official metric per task. Features: Morgan/ECFP4 2048-bit + 17 RDKit physchem; model: HistGradientBoosting (seed=42). **All 22 tasks beat the trivial baseline by >1 sd** (predict-mean / base-rate); **median 81.6% (mean 73%) of the trivial→published-SOTA gap closed** (normalized cross-metric). Near-SOTA: bbb AUROC 0.893 (SOTA 0.924), solubility MAE 0.821 (0.741), dili 0.911 (0.956), cyp3a4_veith PR-AUC 0.866 (0.916), hia 0.950 (0.993), caco2 MAE 0.291 (0.256). Honestly weak but still >trivial (hard/small datasets): half_life spearman 0.168 (SOTA 0.576), vdss 0.270 (0.713), bioavailability AUROC 0.700 (0.942). **Scope (honest):** an in-silico SCREENING FILTER, scaffold-split generalization to novel chemistry only; **NOT** a safety guarantee, clinical, or regulatory determination; per-task applicability-domain (Tanimoto) flag emitted (NOTE: the AD flag is not yet error-validated — B30b follow-up). NO leaderboard-win claim. **SHIPPED** as `intercepta.admet.ADMETPredictor` + CLI `intercepta admet`. Reproduced ×2 byte-identical (payload sha256 af66698f…). | B30_metrics.json, leaderboard_ref.json |
| B30b (module hardening, validated ×2) | Closes two gaps in B30: (i) VALIDATES the applicability domain, (ii) adds CALIBRATED conformal uncertainty. Honest split verdict. | TDC ADMET (22 tasks), official 5-seed protocol: train on train, calibrate inductive conformal on valid, evaluate on scaffold test. **Aim 1 (AD validity):** per-molecule error rises with Tanimoto AD distance in **20/22 tasks (15/22 BH-FDR<0.05), mean Spearman +0.128** → AD DISTANCE is a genuine (weak) continuous reliability weight; the binary 95th-pct OOD flag is a WEAKER separator (13/22 tasks OOD-worse, one-sided Wilcoxon p=0.049) → kept as a soft confidence weight, NOT a hard gate (honest effect-size call: informative but weak; cf. B6/V13). **Aim 2/3 (conformal coverage):** inductive split-conformal (regression |resid| intervals; classification LAC prediction-sets) achieves empirical coverage within ±0.05 of nominal on the scaffold test for BOTH modalities at 90% and 80% — regression 0.906/0.813, classification 0.899/0.786 — i.e. calibrated EVEN under scaffold covariate shift; AD-adaptive (Mondrian) conformal also passes and widens intervals for out-of-domain molecules. Intervals are WIDE (honest: point predictions carry real uncertainty). **SHIPPED:** `ADMETPredictor(conformal=True)` emits AD-adaptive pi_low/pi_high (regression) or conformal_set/set_size (classification); CLI `intercepta admet --conformal`. Reproduced ×2 byte-identical (payload sha256 50cc195c…). Scope unchanged: in-silico screening filter, scaffold-split only, NOT a safety guarantee. | B30b_metrics.json |
| B31 (module, validated ×2 — screening proxy, NOT a lab guarantee) | A structure-only synthesizability predictor is a genuine, honestly-benchmarked screening filter — pipeline module #5 (retrosynthetic SOLVABILITY from SMILES). Beats trivial + the SAscore heuristic on both random and scaffold splits; near published RAscore. | OPEN RAscore/ChEMBL labels (Thakkar et al. Chem Sci 2021; AiZynthFinder + USPTO templates; 179,413 train / 19,935 test; label = route-found, 75.2% positive). Unified featurizer w/ ADMET (Morgan/ECFP4 2048-bit + 17 physchem); HistGradientBoostingClassifier (seed=42); 50k seeded training subsample. **Random split:** AUROC **0.911**, AUPRC 0.966 vs oriented-SAscore 0.847 and trivial 0.5 (published RAscore XGB 0.95 / DNN 0.93, same model family — we trail by 0.04, no SOTA claim). **Bemis–Murcko scaffold split (novel chemistry, 48,778 scaffolds, 3 seeds):** AUROC **0.908±0.002**, AUPRC 0.965 — beats SAscore (0.844) + trivial by >1 sd. **Honest generalization gap random→scaffold = 0.003** (synthesizability is far more structure-local/learnable than the +0.212 drug-response signal). **Scope (honest):** predicts ALGORITHMIC retrosynthetic solvability (USPTO templates + fixed stock) — a computational PROXY for synthesizability, NOT a guarantee a molecule can be made, dependent on the CASP tool/templates baked into labels; scaffold-split generalization. **SHIPPED** as `intercepta.synth.SynthesizabilityScorer` (+ RDKit SAscore + Tanimoto AD + conformal set) + CLI `intercepta synth`. Reproduced ×2 byte-identical (payload sha256 c6edd9bc…). | B31_metrics.json |
| B32 (integration MVP — first-class NEGATIVE) | "Whole > parts" is FALSIFIED on this test: naively composing the module scalar outputs does NOT beat the single best module for a held-out real-world developability outcome. The modules are individually useful; late-fusion of their scalar outputs adds nothing here. | Held-out **ClinTox** (TDC/MoleculeNet; FDA-approved vs failed-clinical-trials-for-toxicity; 1,459 unique, 103 positive/7%). Features = predicted outputs of the B30 ADMET panel (herg, ames, dili, ld50, cyp3a4_veith, bioavailability, bbb, ppbr, clearance_microsome, half_life) + B31 solvable_prob + SAscore, each module trained ONLY on its own data. **Leakage-controlled** (excluded 420/1459 ClinTox molecules present in any module's training set → 1,039 leakage-free), Bemis–Murcko scaffold split, 5 seeds. **Composite (L2-logistic on the 12 module outputs): AUROC 0.819±0.011** vs **best single module output ppbr_az 0.831** (dili 0.782, cyp3a4 0.743, ld50 0.722 also strong) → **H1 (composite > best single by >1sd) FALSE.** H2 holds (composite 0.819 > 0.6 — real developability signal transfers). A direct Morgan-2048 GBT trained ON ClinTox scores 0.857 (beats both — raw structure carries more than the scalar module outputs). **Honest conclusion:** on this benchmark the single best ADMET endpoint is as predictive as composing them; the "platform whole>parts" claim does not hold for naive late-fusion of scalar outputs (richer feature-level fusion is the indicated future direction). Modules ship standalone; `intercepta.integrate.DevelopabilityPrioritizer` is retained as an honest per-molecule PROFILE aggregator (NOT a validated improvement over the best single endpoint) + CLI `intercepta prioritize`. Reproduced ×2 byte-identical (payload sha256 81996f21…). Scope: research prioritization only; small positive class; survivorship-confounded; NOT a clinical/regulatory determination. | B32_metrics.json |
| B32b (feature-level fusion — WEAK/NON-DECISIVE positive, not a validated win) | Follow-up to B32: instead of fusing module SCALAR outputs, fuse the module transfer-features WITH raw structure and test against raw structure alone. Result is a small, consistent, sub-threshold gain — honestly NOT decisive. | Same held-out ClinTox, same leakage control (excluded 420/1459 → 1,039 leakage-free), Bemis–Murcko scaffold split, 5 seeds. SAME model class (HistGradientBoostingClassifier, seed=42) on three feature sets: **S = raw structure** (Morgan/ECFP4 2048-bit + 17 physchem, 2065-dim), **M = 12 module transfer features** (B30 ADMET panel preds + B31 solvable_prob + SAscore), **S+M = concat** (2077-dim). **AUROC: S+M 0.920±0.014 > S 0.906±0.023 > M 0.835±0.031 > best-single-module 0.831.** So feature-level fusion IS the best model (H2 pass) and beats raw structure by **Δ+0.013 with lower variance** — but the margin **does NOT clear the pre-registered >1 sd bar (H1 FAIL)**, so per the fixed decision rule this is **hypothesis-generating, NOT a validated improvement** — do not ship fusion as a win. **Honest net across B32+B32b:** the external-data-trained ADMET/synth modules add at most a small, non-decisive transfer signal on top of raw structure for ClinTox; raw structure is a very strong baseline (0.906). The modules stand as validated STANDALONE tools (B30/B31); the "platform whole>parts" claim is not established on this outcome (feature-fusion is directionally better than B32's scalar late-fusion, which had lost to the best single module). A larger/multi-outcome benchmark or a learned joint representation would be needed to establish a decisive integration win. Reproduced ×2 byte-identical (payload sha256 7d7a305c…). | B32b_metrics.json |
| B33 (module, validated ×2 — optimization demo, NOT drug discovery) | A goal-directed molecular DESIGN module (pipeline #2): a BRICS fragment-recombination genetic algorithm optimizes a developability objective over known chemistry, beating both baselines, at 100% validity — and it exposes single-objective reward-hacking. | RDKit-only (BRICS recombination + QED + Contrib SAscore), deterministic seed=42, ChEMBL seeds; no deep learning / no model fitting. Objective = **multi = QED × (10−SAscore)/9** in [0,1]. **Goal-directed GA beats BOTH baselines:** mean developability F **0.715** (best 0.879) vs ChEMBL seed population **0.427** and no-selection random recombination **0.167**, at **validity 1.0** (valid by construction), uniqueness 1.0, **novelty 1.0 vs seeds**. Honest incidental finding: unconstrained random recombination is WORSE than curated ChEMBL (0.167 < 0.427), so the gain is from selection pressure — the prereg's literal "GA>random>seed" chain is therefore false (random<seed); the adjudicated hypothesis is "GA beats BOTH baselines", which holds decisively (recorded transparently). **Reward-hacking (H2 confirmed):** a single-objective QED-only GA reaches high QED but is LESS synthesizable (mean SAscore 2.40) than the multi-objective GA (2.10) — naive single-objective optimization sacrifices synthesizability, so the multi-objective is necessary. **SHIPPED** `intercepta.generate.MoleculeOptimizer` + CLI `intercepta generate`. Reproduced ×2 byte-identical (payload sha256 d91f0470…). **SCOPE (honest):** goal-directed OPTIMIZATION of QED/SAscore proxies over KNOWN chemistry via fragment recombination — a design/optimization demonstration, NOT de novo discovery of real, better, or practically-synthesizable drugs; every output is a computational hypothesis, not a validated molecule. | B33_metrics.json |
| B34 (module #1 target-ID — POSITIVE, popularity-controlled) | Target-identification evidence predicts which target-disease pairs reached the clinic **beyond a study-popularity baseline** — a genuine, confound-surviving signal (the B10-style test, here PASSED rather than nulled). | OPEN Open Targets Platform v26.06 (fetched by us; 12,000 target-disease pairs across a fixed 40-disease panel; cached, sha256 d14006e8…, MANIFEST). Label = `clinical` datatype > 0 (reached clinic; 29.3% positive). Features = 6 NON-clinical datatypes (genetic_association, genetic_literature, somatic_mutation, affected_pathway, animal_model, rna_expression); `clinical` excluded (leakage). **Leave-DISEASE-out GroupKFold(5)** (generalize to unseen diseases), L2-logistic, seed=42. **Result:** study-popularity baseline (`literature`-only) is near-chance **AUROC 0.522**; **genetic_association alone 0.741**; **full non-clinical 0.839** (AUPRC 0.754 vs prevalence 0.293); adding literature on top does not help (0.832). So clinic-reached-target prediction is driven by genuine biological/genetic evidence, NOT study bias — survives the popularity control (consistent with Nelson 2015's genetic-support effect). **Prereg-adjudication note (transparent):** H2 was pre-registered as h2a∧h2b∧h2c; the signed-coefficient sub-test **h2c FAILS** (genetic_association joint coef −1.69 despite 0.741 marginal AUROC) — a **suppression/multicollinearity artifact** — so H2 is adjudicated on the robust AUROC criteria (h2a genetic-only≫0.5; h2b full≫popularity), both passing by wide margins; h2c reported, not gating. **SCOPE (honest):** conditional on top-300-associated targets per disease (a 'reasonably-associated' sample, not all genes); Open Targets scores are curated evidence; association ≠ validated target; leave-disease-out enrichment ≠ prospective success. Not shipped as a separate tool (would duplicate Open Targets' own evidence scores); the validated FINDING is the deliverable. Reproduced ×2 byte-identical (payload sha256 6c4b5e81…). | B34_metrics.json |
| B35 (integration, rigorous PAIRED re-adjudication — SUGGESTIVE but NOT decisively established) | Fixes B32b's wrong (unpaired ">1sd") test with proper paired statistics. Two rigorous tests bracket the truth and DISAGREE informatively; the honest verdict is "small real-ish gain, not decisively established — power-limited." | Same ClinTox / features as B32b (S=Morgan2048+physchem, M=12 module transfer features, S+M=concat; leakage-controlled ~1,039 pairs, ~70 positive; HGB). **Per-seed paired test (10 scaffold splits):** ΔAUROC(S+M−S) mean **+0.0185**, 80% seeds positive, **Wilcoxon one-sided p=0.019, bootstrap-over-seeds CI [0.0054,0.0320] excludes 0** → significant. **Pooled out-of-fold molecule-level bootstrap (same-data, conservative):** Δ +0.0078, **CI [−0.020,0.039] includes 0, p=0.30** → NOT significant. Pre-registered H1 required BOTH → **H1 FALSE**. **Honest interpretation:** feature-level integration gives a small, consistent-across-splits gain (per-seed significant) that does NOT survive the conservative molecule-level bootstrap because the positive class is tiny (~70) — i.e. the limiter is STATISTICAL POWER, not absence of signal. So "platform whole>parts" is **suggestive but not decisively established** on ClinTox (a sharper resolution of the B32/B32b bound). No validated `fusion=True` shipped (bar not met). **Indicated next step:** a MULTI-OUTCOME integration benchmark (more held-out developability endpoints) to gain the power the single small-positive-class outcome lacks. Reproduced ×2 byte-identical (payload sha256 04c20605…). | B35_metrics.json |
| B36 (multi-outcome integration — WELL-POWERED DECISIVE NEGATIVE; closes the thread) | The definitive integration test: does feature-level fusion (S+M) beat raw structure (S) across MANY held-out outcomes (the power B35 lacked)? It does NOT — decisively. | 7 held-out binary outcomes (clintox, skin_reaction, carcinogens_lagunin, tox21 {NR-AR, NR-ER, SR-MMP, SR-p53}), all distinct from module training endpoints, leakage-controlled, seeded cap 4000 mols (several outcomes with 90–340 positives — far more power than ClinTox's ~55). Same features (S=Morgan2048+physchem; M=10 ADMET-panel preds + synth + SA; S+M=concat) + HGB, scaffold split. Per-outcome ΔAUROC(S+M−S): clintox +0.024, skin +0.003, carcinogens **−0.039**, NR-AR **−0.065**, NR-ER +0.005, SR-MMP +0.006, SR-p53 +0.006. **Meta-analysis across outcomes: mean Δ −0.009; 0/7 outcomes individually significant (molecule-level OOF bootstrap); Wilcoxon-across-outcomes p=0.47; combined bootstrap CI [−0.031,+0.010] includes 0 → H1 FALSE.** **Decisive conclusion:** feature-level integration does NOT reliably beat raw structure — and B35's ClinTox per-seed +0.019 did NOT replicate across the better-powered outcomes (a good illustration of why single-outcome signals need multi-outcome confirmation). The INTERCEPTA platform's value is in its **STANDALONE validated modules** (#1 target-ID, #2 design, #3 efficacy, #4 ADMET, #5 synthesizability), NOT in late/feature fusion of module outputs. No `fusion=True` shipped (bar decisively not met). Integration thread CLOSED as a well-powered, honest negative. Reproduced ×2 byte-identical (payload sha256 ea088da2…). | B36_metrics.json |
| B37 (learned joint representation — MIXED / within-domain only, not a robust win) | The last open integration path: instead of fusing frozen module OUTPUTS, LEARN a joint representation (multi-task neural net) and test transfer to held-out outcomes. Result: within-domain transfer is real, but no general gain over raw structure. | A multi-task MLP (sklearn `MLPClassifier`, shared hidden layers `(256,64)`, seed=42) pretrained on **tox21** (12 assays, 7,831 molecules, Morgan2048+physchem input); last hidden layer = the **learned joint embedding** (64-d). Transferred to 3 held-out outcomes (leakage-excluded vs the tox21 pretraining set): compared HGB on **A=raw structure**, **B=embedding**, **C=structure+embedding**, scaffold split, 5 seeds. **Per-outcome ΔAUROC(C−A):** skin_reaction **+0.063** (genuine within-tox-domain transfer), carcinogens +0.002, clintox **−0.011**; mean +0.018 (2/3 positive) → **H1 (robust win) FALSE**. Embedding ALONE is markedly WORSE than structure (mean ΔB−A **−0.11**), i.e. it discards structural information. **Honest conclusion:** a shallow learned joint representation over Morgan+physchem transfers only WITHIN its training domain (tox→tox, skin_reaction) and does NOT robustly beat raw structure across held-out outcomes — the bottleneck is INFORMATION, not representation, at this scale. Consistent with B32/B36. A robust integration win would require a much larger DEEP molecular foundation model (bigger corpus, learned fingerprints) — stated as the honest boundary, not attempted-and-overclaimed. **SCOPE:** shallow sklearn MLP, one tox pretraining corpus, missing tox21 labels filled 0, 3 small held-out outcomes. Reproduced ×2 byte-identical (payload sha256 c8d16b06…). | B37_metrics.json |
| B38 (deep molecular foundation model — does NOT robustly beat raw structure; CLOSES the integration ladder) | The deepest integration test: a pretrained deep FM (learned fingerprints) instead of fixed Morgan. Result: no robust gain over classical structure — consistent across the whole B32→B38 ladder. | Pretrained **ChemBERTa-77M-MLM** (RoBERTa, unsupervised on 77M molecules; HuggingFace) → 384-d mean-pooled embedding (deterministic, eval mode, rounded 5dp; unsupervised pretraining ⇒ no label leakage). 7 held-out outcomes (clintox, skin_reaction, carcinogens_lagunin, tox21 {NR-AR, NR-ER, SR-MMP, SR-p53}; cap 4000). HGB on A=structure (Morgan2048+physchem), B=FM embedding, C=both; scaffold split. **Per-outcome ΔAUROC(C−A):** skin +0.058, NR-AR +0.030 (only 1/7 individually significant), NR-ER +0.023, SR-p53 +0.035, clintox +0.004, SR-MMP −0.007, carcinogens **−0.191** (small-data overfit). **Meta:** mean ΔC−A **−0.007**, FM-alone ΔB−A −0.034 (worse than structure), 5/7 positive but Wilcoxon p=0.23, bootstrap CI [−0.072,+0.036] includes 0 → **H1 FALSE.** Like B37, the FM gives small WITHIN-tox-domain transfer (skin, tox21) but does NOT robustly beat classical fingerprints and can hurt out-of-domain — consistent with the literature (ChemBERTa ≈/< fingerprints on ADMET/tox). **Decisive across the integration ladder (B32 scalar-fusion → B32b/B35 feature-fusion → B36 multi-outcome → B37 shallow learned rep → B38 deep FM):** no integration approach robustly beats raw structure on these outcomes → the bottleneck is INFORMATION/DATA, not representation. INTERCEPTA's value is its STANDALONE validated modules. **SCOPE:** off-the-shelf FM, one pooling/embedding choice, small held-out positive classes. Reproduced ×2 byte-identical (payload sha256 c17e47f4…). | B38_metrics.json |
| B39 (end-to-end discovery pipeline — POSITIVE: the assembled platform works) | The validated modules composed into a working generate→screen→rank pipeline; it produces valid, novel, synthesizable, predicted-safe candidate molecules beating the starting population. NOT the (falsified) whole>parts predictor claim — the modules used for their intended purpose as a generator + filters. | `intercepta.discover.DiscoveryPipeline`: a BRICS goal-directed GA (design, B33) optimizing **F = QED × synthesizability((10−SA)/9, B31) × safety(1−mean P_tox over herg/ames/dili, B30)** over 200 ChEMBL seeds; deterministic seed=42. **Result:** GA population mean developability **F 0.504** (best 0.627) vs ChEMBL seed population **0.185** (best 0.521) — **H1 PASS** — at **validity 1.0, uniqueness 1.0, novelty 1.0** vs seeds. **H2 PASS (multi-objective matters):** vs a QED-only GA, the multi-objective candidates are **more synthesizable** (mean SAscore 2.16 vs 2.40) AND **safer** (predicted-safety 0.80 vs 0.67) — the synth/safety objectives change the output, not just QED. **56% of top candidates in the ADMET applicability domain** (honest: ~44% are novel chemistry OOD for the ADMET models → their safety calls are unreliable there — the pre-registered gaming caveat, reported transparently). **SHIPPED** `intercepta discover` (CLI): generate→ADMET/synth screen→rank with full per-candidate profile + AD flags. **SCOPE (honest):** computational PRIORITIZATION over KNOWN chemistry (fragment recombination); every candidate is a hypothesis, NOT a validated, novel, safe, or practically-synthesizable drug, and NOT a clinical/safety determination; optimizing against in-silico predictors invites gaming. Reproduced ×2 byte-identical (payload sha256 23a7ae0c…). | B39_metrics.json |
| B40 (target-conditioned generation — POSITIVE: steers to a target, with an honest safety trade-off) | The pipeline can now aim at a chosen disease/target: a validated activity QSAR conditions the discovery GA, genuinely steering candidates toward the target — while surfacing a real activity-vs-safety trade-off. | Target = **HIV** replication inhibition (TDC HTS `hiv`; QSAR trained on 1,443 actives + 10,000 sampled inactives). **QSAR gate PASSED:** Bemis–Murcko scaffold-split AUROC **0.806** (a real activity predictor). Objective = developability (B30/B31) × **P(HIV-active | QSAR)**; compared vs the unconditioned pipeline (B39, developability only), same seeds. **H1 (steers) PASS:** conditioned mean predicted P(HIV-active) **0.164 = 3.96× the unconditioned GA (0.041) and 1.81× the ChEMBL seeds (0.091)** — conditioning genuinely biases generation toward the target — at **validity 1.0, novelty 1.0**, preserving drug-likeness (QED 0.771 ≥ unconditioned 0.721) and synthesizability (SAscore 2.27). **H2 (developability fully preserved) FALSE — honest trade-off:** predicted SAFETY drops to **0.434** (vs 0.804 unconditioned) — HIV-active-like chemistry is predicted more toxic; an activity-vs-safety tension, transparently reported (the objective can be re-weighted toward safety). 63% of candidates in the ADMET applicability domain. **SHIPPED** `intercepta discover --target-hts <name>` + `DiscoveryPipeline(target_model=…)` / `build_target_qsar`. **SCOPE (honest):** activity is QSAR-PREDICTED not measured; optimizing against a QSAR invites gaming; candidates are hypotheses over KNOWN chemistry, NOT validated actives, novel drugs, or safe/synthesizable-in-practice molecules; no clinical/efficacy claim. Reproduced ×2 byte-identical (payload sha256 e62417bf…). | B40_metrics.json |
| B41 (AD-constrained Pareto discovery — PARTIAL: reliability win + honest tensions) | Applies the literature-correct fixes to B40 (NSGA-II Pareto instead of scalarization; applicability-domain reliability constraint vs reward-hacking; Fromer&Coley 2022, Nat Commun 2025). Reliability improves decisively; but exposes real reliability↔activity and Pareto↔scalar tensions. | 3 arms, same HIV objective as B40 (activity, safety, QED, synth kept SEPARATE for Pareto), BRICS-GA pop=100 gen=8. **H1 (reliability) PASS:** AD constraint raises the in-domain (reliable-prediction) fraction from **0.54 (scalar/unconstrained ≈B40) → 0.92 (Pareto+AD)** — largely eliminating the OOD reward-hacking B40 flagged. **H2 (diversity/balance) PASS:** Pareto yields a far wider non-dominated frontier (front size 19→**119**) of activity/safety/developability trade-off candidates, incl. active-AND-safe points. **H4 (Pareto hypervolume ≥ scalar) FALSE:** the scalar arm CONCENTRATES higher dominated hypervolume (0.329 vs Pareto 0.20–0.21) by driving toward one high-product corner, whereas Pareto spreads across the frontier — so Pareto gives DIVERSITY+RELIABILITY, not a hypervolume win here. **Honest tensions surfaced:** (a) **reliability trades off against activity** — in-domain candidates are LESS predicted-active (mean activity 0.064 for arm C vs 0.169 scalar) because active-like NOVEL chemistry is out-of-domain; you can be reliable OR chase (unreliable) high activity, not both. (b) The activity↔safety correlation is **−0.13 within the reliable (AD) set vs −0.38 unconstrained** → B40's strong safety trade-off was **partly an OOD artifact, partly real**. **Net:** the AD-reliability constraint is a genuine, validated improvement (reward-hacking mitigated, 0.92 in-domain); Pareto adds diversity; but "better candidates" is a multi-objective judgement, not a single-number win — reported honestly, no overclaim. `ParetoOptimizer` shipped in `intercepta.generate` (additive; B33 re-verified byte-identical d91f0470). Reproduced ×2 byte-identical (payload sha256 359486bf…). SCOPE: all objectives in-silico predictions; AD improves reliability not ground truth; candidates are hypotheses, not validated drugs. | B41_metrics.json |
| B42 (retrospective known-drug re-discovery — POSITIVE against EXTERNAL truth; the credibility test) | The first validation against REAL known drugs/actives (not our own models) — directly attacking the circular-validation threat. Both arms pass, honestly bounded. | **Arm 2 (scoring enriches REAL actives):** HIV (TDC HTS), Bemis–Murcko scaffold split ×5, real actives vs decoys, standard retrospective-VS metrics (rdkit.ML.Scoring): **AUROC 0.806, BEDROC(α=80.5) 0.938, EF@1% 7.40×, EF@5% 6.74×** (prevalence 0.126) → the activity scoring enriches real known actives ~7× in the top 1% (strong EARLY recognition, not chance) — **H2 PASS.** **Arm 1 (generator rediscovers known drugs, GuacaMol-style):** BRICS-GA optimizing Tanimoto(ECFP4) to each target drug (seeds = 2,000 ChEMBL, target excluded); **3/3 reached analog-level similarity** — celecoxib 0.60, troglitazone 0.57, thiothixene 0.48 — each **improving +0.18–0.25 over the best seed-pool molecule** → the generator reaches the right chemical NEIGHBORHOOD of real drugs — **H1 PASS.** **Honest bound:** rediscovery is ANALOG-level (~0.5–0.6 Tanimoto), NOT exact reconstruction (fragment-recombination has limited reach vs graph-crossover GAs); enrichment is on ONE target (HIV); retrospective + in-silico; **similarity ≠ proven activity**; not wet-lab. **Significance:** this is the strongest computational-validation evidence in the program — the pipeline recovers EXTERNAL truth (real actives enriched, real drugs' neighborhoods reached), the key credibility gap flagged as threat #1. Reproduced ×2 byte-identical (payload sha256 9d99060e…). | B42_metrics.json |
| B43 (generality across a target panel — POSITIVE: enrichment is not target-specific) | Applies B42's external-truth VS-enrichment protocol across a diverse 6-target panel; it generalizes across every target class. | 6 TDC HTS single-target datasets spanning antiviral/GPCR/ion-channel/kinase/protease; per target: QSAR (all actives + ≤6,000 sampled inactives), Bemis–Murcko scaffold split ×3, real-actives-vs-decoys AUROC/BEDROC(80.5)/EF (rdkit.ML.Scoring). **Result: 6/6 targets enrich (AUROC>0.7 AND EF@1%>3)** — HIV 0.82/EF 5.0×, m1-muscarinic(GPCR) 0.82/8.8×, orexin-1(GPCR) 0.83/11.1×, Kir2.1(ion-channel) **0.89**/19.2×, SARS-CoV-2-3CLpro(protease) 0.76/9.5×, STK33(kinase) **0.89**/25.8×. **Panel-mean AUROC 0.835 (range 0.76–0.89), mean BEDROC 0.571, mean EF@1% 13.2×** — H1 (generalizes to ≥4/6) and H2 (mean BEDROC>0.3) both PASS. The scoring's real-active enrichment is **general across target classes, not HIV-specific**. **SCOPE (honest):** retrospective, in-silico, real-actives-vs-decoys enrichment on scaffold splits — NOT prospective, NOT wet-lab; low-active HTS screens (78–172 actives) are harder but still enrich; enrichment ≠ proven activity. No src/ change. Reproduced ×2 byte-identical (payload sha256 daca99a2…). | B43_metrics.json |
| B44 (ligand-based 3D scaffold hopping via RDKit O3A — NEGATIVE, honest; bounds the last feasible computational rung) | Tests the ONE retrieval task where 3D shape/pharmacophore can, in principle, beat 2D fingerprints: retrieving actives on a NOVEL scaffold (2D-dissimilar to the reference actives — "scaffold hopping"). No receptor docking engine is installed (Vina/smina/gnina absent; only Open Babel), so this ligand-based 3D probe is the last computationally-feasible rung before the structure-based-binding wall. | Target = **HIV** (TDC HTS). 8 references = one representative from each of the 8 largest Murcko-scaffold clusters; each query molecule embedded to ONE ETKDGv3 conformer (fixed seed) + MMFF94-optimized, then aligned to every reference with **RDKit O3A** (shape+pharmacophore), 3D score = max O3A over references; 2D baseline = max Morgan Tanimoto over the same references. 3,802/3,935 molecules embedded (133 failures); 1,181 novel-scaffold actives. **Retrieval on novel-scaffold actives vs decoys: 3D AUROC 0.634 vs 2D 0.589 (Δ +0.045 — 3D ranks scaffold-hops modestly better GLOBALLY), BUT early enrichment is NOT improved: EF@1% 3D 2.54× vs 2D 2.71×, BEDROC(80.5) 3D 0.738 vs 2D 0.803 (3D slightly worse).** Pre-registered **H1 (conjunction: ΔAUROC>0.03 AND EF@1%_3D>EF@1%_2D) FALSE** — it fails cleanly on the early-recognition clause, which is the operationally-decisive one for screening. **H2 (3D meaningful: AUROC>0.6 AND EF@1%>2) TRUE.** Full-set context (all actives vs decoys, where 2D is expected to hold): 2D AUROC 0.625 / BEDROC 0.959 vs 3D 0.651 / 0.878 — same pattern (3D marginally better global rank, worse early recognition). **Honest reading:** single-conformer ligand-based 3D adds a small global-ranking edge on scaffold hops but **no early-enrichment gain over the 2D Morgan spine** on HIV; the 2D fingerprint remains the operative retrieval tool. **SCOPE:** retrospective, in-silico, single-conformer (an approximation to the true ensemble), O3A is a heuristic overlay not a binding energy, one target, decoys not property-matched, enrichment ≠ proven activity — NOT wet-lab. New experiment file only; no src/ change. Reproduced ×2 byte-identical (payload sha256 1120c3d3…). **Significance: this closes the feasible-computation frontier — receptor docking/MD, efficacy ground truth, and wet-lab are the remaining truth-gaps, which need data/infrastructure decisions, not more modeling.** | B44_metrics.json |
| B45 (honest self-audit: does our enrichment survive on NOVEL chemistry? — POSITIVE with an honest optimism tax) | Turns our rigor on ourselves. B42/B43 used Bemis–Murcko **scaffold splits**, which the literature shows **overestimate** VS performance (arXiv:2406.00873). B45 asks whether the enrichment holds when test compounds are genuinely dissimilar to training. **Transparent self-correction:** the pre-registered Butina cluster split FAILED its own validity check H2 (cross-set NN-Tanimoto 0.428 vs scaffold 0.425 — NOT harder), so I discarded that run and switched to a tuning-free design (documented as a dated amendment in the prereg; original left intact). | **Amended design:** scaffold-split test sets, stratified by each test compound's **max Tanimoto to training (cross-set NN)** into bands [<0.3, 0.3–0.4, 0.4–0.5, ≥0.5]; AUROC/EF@5% per band; the NN<0.4 pool = "genuinely novel chemistry." **Result: capability SURVIVES into novel chemistry (H1′ PASS) with a real optimism tax (H2′ PASS).** Panel-mean AUROC drops from **full-test 0.837 → novel(NN<0.4) 0.786**; **5/6 targets keep novel-band AUROC>0.65** (STK33 0.90, Kir2.1 0.87, m1-muscarinic 0.80, orexin-1 0.76, SARS-CoV-2-3CLpro 0.77). A genuine similarity gradient exists (mean AUROC[≥0.5]−AUROC[<0.3] = **0.159**), confirming scaffold numbers were somewhat optimistic. **Honest weak spot:** **HIV collapses to novel-band AUROC 0.61** — its enrichment is largely similarity-driven, not generalization. **Takeaway (adopted going forward): report the novel-band (NN<0.4) numbers as the honest generalization estimate, not the full-test/scaffold numbers.** SCOPE: retrospective, in-silico, real-actives-vs-decoys; enrichment ≠ proven activity; not wet-lab. No src/ change. Reproduced ×2 byte-identical (payload sha256 e437713b…). | B45_metrics.json |
| B46 (unbiased external benchmark: LIT-PCBA — POSITIVE, honest external footing) | First evaluation of our ligand-based QSAR channel on the community's **unbiased** VS benchmark (LIT-PCBA, Tran-Nguyen et al. JCIM 2020; 15 targets, realistic 1:1000–1:20000 imbalance), under the honest NN<0.4 novel-chemistry lens from B45. "Bar before boast" on external data. | Per target: largest-fragment + canonicalize + **cross-label dedup** (leakage control); all actives + seeded ≤8,000-inactive subsample; Morgan2048+physchem → `admet._TaskModel`; Bemis–Murcko scaffold split ×3; AUROC/BEDROC/EF (rdkit.ML.Scoring) + novel(NN<0.4) AUROC. 5 low-active targets (ADRB2 17, ESR1_ago 13, IDH1 39, OPRK1 24, PPARG 24) **skipped honestly** (<60 actives). **Result (H1+H2 PASS): median full-test AUROC 0.781 (mean 0.796), 9/10 targets >0.70** — FEN1 0.945, VDR 0.871, PKM2 0.864, GBA 0.843, MAPK1 0.787, ALDH1 0.775, MTORC1 0.736, KAT2A 0.735, ESR1_ant 0.729, TP53 0.678. **Survives to novel chemistry: panel-mean novel(NN<0.4) AUROC 0.725** (FEN1 0.895, PKM2 0.787, MAPK1 0.783, VDR 0.768). **Honest reading:** median AUROC 0.78 is competitive with published ligand-based/ML methods on LIT-PCBA (typically ~0.7–0.8); the high EF@1% (median ~13.5) is **inflated by the inactive subsampling and is NOT directly comparable** to published full-ratio EF (Vina ~0.9, GNINA ~2.1, ML ~4–5, arXiv:2605.01681) — AUROC is the fair metric, flagged explicitly. **Honest weak spots:** TP53 full AUROC 0.68 (<0.70); MTORC1 novel<0.4 collapses to 0.52 (similarity-driven). SCOPE: retrospective, in-silico, subsampled ratio, decoys not property-matched; enrichment ≠ proven activity; NO SOTA claim; not wet-lab. No src/ change. LIT-PCBA in MANIFEST (sha 93467a5b, never committed). Reproduced ×2 byte-identical (payload sha256 33d754cd…). | B46_metrics.json |
| B47 (structure-based channel: AutoDock Vina docking — POSITIVE: above-chance + ORTHOGONAL, motivates B48) | Adds the first genuinely NEW information channel in the program — physics-based docking of molecules into the target's 3D pocket (the receptor), vs everything prior being ligand-only. Asks: does docking enrich, and is it orthogonal to the ligand channel? | `docking` env (rdkit 2025.09.5 + openbabel 3.1 + **AutoDock Vina 1.2.7**, seed=42/cpu=8 verified byte-deterministic). 3 LIT-PCBA targets w/ co-crystal receptors (FEN1/MAPK1/ESR1_ant); per target 60 actives + 120 decoys; ligand prep SMILES→ETKDGv3(seed)→MMFF→obabel pdbqt; receptor mol2→pdbqt(-xr); box = co-crystal centroid, 22³Å; dock (exhaustiveness 8) → score = −affinity. Ligand-similarity channel = max ECFP4 Tanimoto to the co-crystal ligand. **Result (H1+H2 PASS): docking enriches above chance (panel-mean AUROC 0.658** — FEN1 0.642, MAPK1 0.623, ESR1_ant 0.709) **and is ORTHOGONAL to the ligand-similarity channel (mean Spearman 0.27)** → complementary information, the prerequisite for a fusion gain (B48). **Honest framing (no overclaim):** docking beat the *unsupervised co-crystal-similarity* baseline (0.51) on 3/3 targets, BUT that similarity baseline is far weaker than our *supervised* QSAR (B46 median AUROC 0.78) — so docking is NOT the strongest channel; its value is the **orthogonal signal**, not raw superiority. Early enrichment is weak (EF@5% 1.3–1.7×), consistent with the literature (docking gives modest global ranking, poor early recognition on unbiased data). SCOPE: heuristic score (not binding ΔG), rigid single-conformation receptor, obabel Gasteiger prep (not AD4/meeko), 3 targets, subsampled; not wet-lab; NO SOTA claim. New experiment file + `docking` env (rdkit/scipy/vina/obabel added); no src/ change. Reproduced ×2 byte-identical (payload sha256 24d5b0f6…). | B47_metrics.json |
| B48 (channel fusion — the payoff — DECISIVE NEGATIVE: integration ceiling holds even with orthogonal 3D info) | The program's payoff test: does fusing the strong ligand-QSAR channel with the orthogonal structure-docking channel (B47) beat the best single channel? Extends the B32→B38 integration negative-envelope to a genuinely NEW information source (the 3D receptor), which the earlier study never had. | Same 3 LIT-PCBA targets/ligands as B47 (FEN1/MAPK1/ESR1_ant; 60 act + 120 dec, eval ligands held OUT of QSAR training — leakage control). 3 channels: **ligand-QSAR** (`admet._TaskModel`, supervised, strong), **ligand-similarity** (ECFP4 to co-crystal), **docking** (Vina −affinity). Fusion = **leakage-controlled logistic (3-fold Bemis–Murcko scaffold-CV out-of-fold)** primary + parameter-free mean-rank secondary; AUROC/EF on full + novel<0.4. **Result (H1+H2 FALSE): fusion does NOT beat best single** — panel-mean AUROC: QSAR **0.835** (best single), docking 0.604, ligsim 0.460; **logistic-fusion 0.821 (Δ −0.014 vs best single, < +0.02 bar)**; mean-rank fusion 0.695 (naive averaging HURTS). On novel chemistry same story (fusion 0.713 vs best 0.740). **Interpretation:** despite docking being orthogonal (B47 Spearman 0.27), it is NOT additive — the QSAR already captures the accessible discriminative signal; orthogonal ≠ complementary-for-enrichment. Nuance: the leakage-controlled WEIGHTED fusion was smart enough to recover ≈QSAR (didn't get hurt), whereas naive equal-weight rank fusion dropped to 0.695. **This is the strongest test yet of the program's central thesis — "the bottleneck is INFORMATION, not combination" — now extended even to orthogonal structure-based information.** SCOPE: retrospective, 3 targets, subsampled, in-silico; docking heuristic/rigid; not wet-lab; NO SOTA claim. First-class negative. New experiment file + `docking` env (sklearn added); no src/ change. Reproduced ×2 byte-identical (payload sha256 68c5b043…). | B48_metrics.json |
| B49 (proteochemometric pan-target — the "any disease" axis — PARTIAL: generalizes, but ligand-driven, protein features add NOTHING) | Can we predict activity for targets with NO ligand data? PCM = ligand ⊕ protein (ESM-2) features, leave-PROTEIN-out. This is the regime B48 did NOT test (data-starved), and the concrete "any disease" generalization axis. | 15 LIT-PCBA target-sets → 14 UniProt proteins (incl. the low-active targets B46 skipped — pooling makes them usable). Ligand Morgan-1024 ⊕ ESM-2 `esm2_t30_150M` mean-pooled embedding (CPU, deterministic, seq≤1022); HistGradientBoosting; **leave-protein-out CV** (train on 13 proteins, predict the held-out protein's actives-vs-decoys). Baseline = ligand-only (no protein features). **Result: H1 TRUE (barely), H2 FALSE.** Leave-protein-out mean PCM AUROC **0.599** (10/15 unseen targets >0.60) — so there IS weak cross-target generalization — **BUT ligand-only pooled scores HIGHER (0.636); protein features Δ −0.038 (they slightly HURT).** The ESM-2 embedding adds **no usable target-specific signal** over pooled ligand structure (literature-consistent: PLM embeddings capture limited target-specific bioactivity). **Honest reading:** cross-target generalization exists but is carried by **ligand chemistry, not the protein representation** — the "any disease via protein features" route is falsified in this setup; a pooled ligand model already captures a general "active-like" signal but cannot be made target-specific by naive ESM concatenation. Best transfer: FEN1 (PCM 0.803); worst: OPRK1/ADRB2 (~0.36–0.39, below chance — tiny-active GPCRs). SCOPE: retrospective, in-silico, 14 proteins, seq truncated to 1022, decoys not property-matched; enrichment ≠ proven activity; not wet-lab; no SOTA claim. First-class result. ESM-2/UniProt in MANIFEST (open, never committed); no src/ change. Reproduced ×2 byte-identical (payload sha256 755706ee…). | B49_metrics.json |
| B51 (closed-loop in-silico DMTA / active-learning engine — FLAGSHIP — POSITIVE: the loop works) | The one lever left after B48 (can't combine fixed scores) and B49 (can't represent past it): change WHICH data you acquire. Simulates the Design–Make–Test loop against a HIDDEN real-bioactivity oracle — turning the static scorer into a data-acquisition engine (the truest in-silico miniature of the fullest-vision workflow). | 3 LIT-PCBA targets (FEN1/MAPK1/ALDH1); oracle pool = 300 actives + 10,000 inactives (~2.9% hit rate), labels hidden, revealed only on "test". Pool-based AL: seed 100 random → 15 rounds × (train HGB on labelled → score pool → acquire batch 100 → reveal labels). 4 acquisition strategies under identical seeds (×3): **random, greedy (max P), uncertainty (max P(1−P)), UCB (P+κ√P(1−P))**. **Result (H1+H2+H3 ALL PASS): closed-loop discovery WORKS** — at a fixed 1,600-compound budget (~15% of pool) model-guided AL recovers **~2.8× more real actives than random** (UCB 2.83×, greedy 2.82×, uncertainty 2.78×), strongly target-dependent (FEN1 ~4.8×: 240 vs 50 found; MAPK1 ~2.3×; ALDH1 ~1.4×). **H2 (recall):** UCB ≥ greedy ≥ uncertainty (exploit/hybrid win pure recall). **H3 (generalisation):** exploration pays off — untested-pool end-model AUROC uncertainty **0.602** > greedy 0.582. **Honest reading:** the big, robust win is **model-guided ≫ random**; the strategy differences are small but in the literature-predicted direction (UCB/greedy best for hit recall, uncertainty best for model generalisation — the explore/exploit tradeoff, characterised). This is the first result demonstrating INTERCEPTA as an **engine/loop**, not static modules. SCOPE: in-silico DMTA proxy on REAL labels (not a live assay), pool subsampled, single-model entropy uncertainty, 3 targets; finding actives fast ≠ a drug; enrichment ≠ proven activity; NOT wet-lab. New experiment file; no src/ change. Reproduced ×2 byte-identical (payload sha256 e71129f4…). | B51_metrics.json |
| B52 (de novo generator on GuacaMol goal-directed vs published SOTA — HONEST CALIBRATION: optimises but below SOTA) | External SOTA footing for the design module (B33 BRICS-GA) on the community-standard GuacaMol goal-directed benchmark (Brown et al. JCIM 2019), using the EXACT published scoring functions (guacamol 0.5.2 + scipy.histogram compat shim; no FCD). | 6 canonical goal-directed tasks; `intercepta.generate.MoleculeOptimizer` wrapped as a guacamol GoalDirectedGenerator over 3,000 ChEMBL seeds (pop≥120, 15 gen, seed=42). **Result (H1 PASS, H2 PASS-as-expected): the GA genuinely optimises but is BELOW SOTA** — mean score **0.568 vs published Graph-GA 0.893** (gap −0.325), clears the Best-of-Dataset floor on 2/6. Per task (ours / Graph-GA / Best-of-Dataset): Osimertinib MPO **0.802**/0.953/0.839, Fexofenadine MPO 0.755/0.998/0.817, Celecoxib rediscovery 0.667/1.000/**0.505✓**, Aripiprazole similarity 0.650/1.000/**0.595✓**, Median molecules 0.280/0.438/0.334, C11H24 isomers **0.258**/0.971/0.684. **Honest reading:** the fragment-recombination GA is a real optimiser (best relative on MPO, ~0.8) but cannot match graph-GA/LSTM SOTA — the gap is largest exactly where **exact structural reconstruction** is required (isomers 0.258, rediscovery/similarity ~0.66), consistent with B42's analog-level reach; the design module would need a stronger generator (graph-GA / SMILES-LSTM / RL) to close it. SCOPE: 6/20 tasks, in-silico; outputs are computational hypotheses NOT validated molecules; published baselines hard-coded (minor version variance); NO SOTA claim; not wet-lab. New experiment file + guacamol in `docking` env; no src/ change. GuacaMol/ChEMBL in MANIFEST. Reproduced ×2 byte-identical (payload sha256 d67f949d…). | B52_metrics.json |
| CONSOLIDATION (ship the engine: `intercepta screen`) — engineering, no new scientific claim | Composes the validated pieces into one usable virtual-screening ENGINE: calibrated ligand-QSAR (Morgan+physchem→HGB) + Tanimoto **applicability-domain** gate + AD-adaptive **conformal** uncertainty (the `_TaskModel` machinery validated in B30b/B42/B46) + the **B51 active-learning loop** (greedy/uncertainty/UCB batch selection). | SHIPPED `intercepta.screen.VirtualScreener` (`.fit`/`.score`/`.active_learning_select`/`.from_hts`) + CLI `intercepta screen` (9th tool). `score()` ranks candidates by P(active) with an AD flag + conformal set; `active_learning_select()` is the reusable B51 loop. STRICTLY ADDITIVE — imports `admet.featurize`/`_TaskModel`, modifies no shared code, so all prior experiments remain byte-identical (no re-verify needed). 33/33 unit tests pass (30 prior + 3 new: fit/score, AL-beats-random, determinism), all data-free. HONEST SCOPE (in docstrings + CLI): retrospective in-silico prioritization; enrichment validated on scaffold/novel-chemistry splits + unbiased LIT-PCBA (~0.78 AUROC, B46) but NOT prospectively confirmed; p_active is a ranking score, not proven activity; out-of-domain rows low-confidence; outputs are hypotheses, not validated drugs; not wet-lab. No experiment/metrics.json (engineering deliverable; correctness = the unit-test suite). REPORT shipped-tools line updated. | (code + tests) |
| B53 (data-regime crossover: when does structure-based information beat ligand-based? — REFINES the theory: conditional crossover GATED BY DOCKING FIDELITY) | THEORY-LEVEL question (defines the boundary of the 'ligand-structure sufficiency' working principle): as known actives N shrink, does N-independent docking overtake the best ligand method, or does ligand-based dominate down to a handful of actives? Gave the competing hypothesis (ligand-similarity) a fair baseline. | 3 LIT-PCBA targets w/ co-crystal receptors (FEN1/MAPK1/VDR); scaffold-held-out novel-chemistry test (50 act + 100 dec); sweep N in {5,10,20,40,80,160} training actives x5 seeds; 3 channels on the SAME test: ligand-QSAR(N) HGB, ligand-similarity(N) max-ECFP4-Tanimoto, and N-INDEPENDENT AutoDock Vina docking (seed42/cpu8). **Result: pre-registered H1 (crossover on >=2/3) FALSE — only MAPK1 crosses (N*~80); but the honest reading REFINES rather than rejects.** The crossover is real yet **conditional on docking being informative for that target**: on MAPK1 (kinase, well-defined pocket) docking AUROC 0.633 BEATS the ligand-QSAR in the scarce regime (QSAR 0.53->0.61 for N<80) until QSAR overtakes at N~80; on FEN1 (0.485) and VDR (0.409) docking is **BELOW RANDOM**, so nothing crosses and ligand wins by docking's FAILURE, not ligand's strength. **New WORKING PRINCIPLE P5 (low confidence): structure-based information helps in the scarce-data regime only where the docking model exceeds random for that target — a strongly target/receptor-class-dependent and often-unmet condition.** Sub-findings: (a) ligand-SIMILARITY was the weakest channel everywhere (0.48-0.76 < QSAR at every N incl. N=5) -> the 'consensus fingerprint stays competitive at low N' claim is WEAKENED; low-N ligand strength comes from the trained model, not similarity. (b) rank-fusion HURT wherever a below-random docking channel was averaged in (H2 FALSE) -> strengthens B48 ('naive fusion cannot rescue; you must know which channel is informative'). **HONEST CAVEAT (Phase 12): B53's power to test the crossover is itself limited by docking fidelity — below-random docking on 2/3 targets most plausibly reflects an accessible-protocol failure (rigid receptor, obabel Gasteiger prep, single box) for those target classes, not anti-information.** SCOPE: retrospective, in-silico, heuristic docking as-configured, 3 targets, novel-chemistry test; enrichment != proven activity; not wet-lab; competing decoy-artifact hypothesis (property-matched decoys) NOT yet excluded. New experiment file; no src/ change. Reproduced x2 byte-identical (payload sha256 1ba9729c...). | B53_metrics.json |
| B54 (factorial decomposition of VS enrichment — decoy-bias vs analog-bias INDEPENDENT; a modest irreducible binding signal) | THEORY: after a focused literature review (property-matched decoys DUD-E/DeepCoy; analog/AVE bias Wallach-Heifets/MUV are established SEPARATELY), the unresolved gap is whether the two biases are independent/additive or the same phenomenon, and what irreducible signal survives BOTH. Unifies P2 (analog inflation) + T2 (decoy artifact) into one law. (Original 'do matched decoys reduce enrichment?' design was discarded pre-commit as reproducing established knowledge.) | 8 LIT-PCBA targets; **2x2 factorial** {A random vs property-matched decoys (greedy NN in z-scored 6-D physchem)} x {B random-split vs novel-chemistry (scaffold + test-active NN<0.4 to train) actives}; Morgan-1024 HGB, AUROC per cell, 5 seeds. **Result (H1 + H3 TRUE, H2 FALSE): the two biases are INDEPENDENT/ADDITIVE and a modest irreducible signal survives.** Panel cells: standard A0B0 **0.790** -> decoy-controlled A1B0 0.724 -> analog-controlled A0B1 0.712 -> doubly-controlled A1B1 **0.628**. Decoy-bias effect **+0.075**, analog-bias effect **+0.087** (comparable), **interaction -0.019 (~0 -> INDEPENDENT)**. **Quantitative law: of the enrichment headroom (0.79->0.5 = 0.29 above chance), ~56% is bias (analog 0.087 + decoy 0.075) and ~44% is irreducible binding signal (0.128); the two artifacts are separate and additive, so honest evaluation needs BOTH controls.** Irreducible A1B1 > 0.60 on only 3/8 targets (FEN1 0.80, PKM2 0.67, VDR 0.66; other 5 near-chance 0.55-0.60). **Theory impact:** P2 strengthened+quantified; NEW WORKING PRINCIPLE P6 (bias-independence/additivity); P1 RECALIBRATED DOWN (real ligand signal ~0.63 panel, far below the biased ~0.79 our B42/B43/B46 reported -> those were single-controlled at most and overstated the binding signal). T2 wild-card PARTIALLY vindicated (large quantified artifact) but its catastrophic form FALSIFIED (residual 0.63>0.5). **HONEST CAVEAT:** 6-descriptor matching is a LOWER bound on decoy artifact (finer matching could shrink the residual further); NN<0.4 is one analog threshold; 8 targets; presumed-negative decoy label noise. SCOPE: retrospective, in-silico; enrichment != proven activity; not wet-lab. No src/ change. Reproduced x2 byte-identical (payload sha256 494d30c7...). | B54_metrics.json |
| B55 (attempted external replication of P6 on DUD-E — INCONCLUSIVE for P6, but a first-class methodological finding about DUD-E) | Graduation test: does P6 (bias independence/additivity, B54) survive a different benchmark family (DUD-E)? Followed Phase-2 review; pre-registered the B54 factorial verbatim on DUD-E (same thresholds). | 8 class-diverse DUD-E targets (egfr/vgfr2/akt1/aa2ar/fa10/hivpr/ppara/gcr). **The pre-registered 2x2 factorial COULD NOT RUN: the novel-chemistry (NN<0.4) analog-control arm is EMPTY on all 8 targets** (0/8 reach >=15 novel test actives). Diagnostic (the finding): DUD-E actives are extremely analog-clustered — panel-mean leave-one-out NN-Tanimoto **0.71**, only **~4.6%** of actives NN<0.4. **Sharper:** every DUD-E active has a DISTINCT Murcko scaffold (unique-scaffold ratio ~1.0) yet mean NN 0.71 -> **SCAFFOLD-SPLITTING DOES NOT CONTROL ANALOG SIMILARITY on DUD-E** ('scaffold-split novelty' is illusory there; a concrete warning to report analog control by explicit NN-distance, not scaffold split, on clustered benchmarks). **Integrity note:** the original factorial run emitted a NaN 'P6 fails' verdict — DISCARDED, not committed (an unsupported conclusion from a null computation); runner refocused to the honest diagnostic; prereg amended (dated), original design intact. **Theory impact:** P6 status UNCHANGED — externally UNTESTED (neither replicated nor falsified); its external replication is deferred to a chemically-diverse benchmark (MUV, built for spread). SCOPE: retrospective, in-silico, 8 targets; not wet-lab. New experiment file; no src/ change. DUD-E in MANIFEST. Reproduced x2 byte-identical (payload sha256 09a0eb27...). | B55_metrics.json |
| B56 (external replication of P6 on the TDC/Butkiewicz HTS panel — P6 REPLICATES; graduates toward a scientific principle) | Correct-instrument graduation test after Phase-2 disqualified DUD-E (B55: too clustered) and MUV (designed-unbiased -> nothing to decompose; only ~30 actives). Verified the TDC/Butkiewicz HTS panel has diverse actives (~46-54% NN<0.4) AND natural biases -> can run the B54 factorial (the B55 lesson enforced). | 6 diverse TDC HTS targets (antiviral/2xGPCR/ion-channel/kinase/protease); B54 2x2 factorial VERBATIM {random vs property-matched decoys} x {random-split vs novel-chemistry NN<0.4 actives}, Morgan-1024 HGB, 5 seeds. **Result (H1 TRUE): P6 REPLICATES — interaction -0.0005 (|.|<0.03; B54 -0.019), biases INDEPENDENT/ADDITIVE on a different curation+target set.** Panel A0B0 0.827 -> A1B0 0.777 -> A0B1 0.727 -> A1B1 **0.677** (doubly-controlled). decoy-effect +0.050 (B54 +0.075), analog-effect **+0.100** (B54 +0.087). 5/6 targets usable (sarscov2 skipped, <15 novel test actives). Per-target interactions all within +/-0.013 (remarkably consistent independence). **Internal-consistency check:** HIV shows negative decoy-effect (-0.020), huge analog-effect (+0.196), residual 0.569~chance -> HIV enrichment is almost ENTIRELY analog-driven, independently REPRODUCING B45's 'HIV is similarity-driven'. **Theory impact:** P6 GRADUATES from working->replicated principle (LIT-PCBA + Butkiewicz HTS; different target classes; mechanistic rationale = the two biases act on geometrically different axes, chemical-similarity vs physicochemical-property). Refinement: **analog bias consistently >= decoy bias** across both families. **HONEST BOUNDARY:** partial externality — both families are PubChem-HTS-derived; P6 NOT tested against a fundamentally different DECOY paradigm (property-matched ZINC like DUD-E) because no benchmark has diverse actives AND that decoy construction (inherent data limit, not laziness). SCOPE: retrospective, in-silico, 5 targets; not wet-lab. No src/ change. Reproduced x2 byte-identical (payload sha256 32469564...). | B56_metrics.json |
| B57 (mechanism of the target-dependent VS residual — HONEST NULL: not explained by SAR ruggedness or other simple properties) | Turns P2/P6 toward a predictive rule: does a target-level property explain WHEN the doubly-debiased binding signal (A1B1) survives? Phase-2 grounded (activity cliffs / SAR tolerance / landscape roughness). | Meta-analysis over the 13 targets with a reproduced A1B1 residual (B54 8 LIT-PCBA + B56 5 TDC-HTS); Spearman(residual, predictor) for 4 target-level predictors. **Result (H1 FALSE — first-class null): NO measured property explains the residual (all |Spearman|<0.5, n=13).** activity-cliff density (the pre-registered hypothesis) = **-0.36** — correct sign (rugged SAR -> lower residual) but WEAK, below the -0.5 bar; assay-type -0.39 (artifact — only 1 phenotypic target); active-diversity +0.28; n_actives +0.09. **Honest reading:** the SAR-ruggedness mechanism is directionally supported but NOT confirmed at this power; the target-dependence of the residual has a different or unmeasured cause. Self-critique (Phase 11): n=13 underpowered (a -0.36 may be a real weak effect), the cliff operationalization is crude (a continuous roughness index e.g. ROGI may correlate better), and 5-seed residual noise attenuates correlations -> 'not confirmed at this power', NOT 'no effect'. **Theory impact:** the ambition to PREDICT when ligand-based VS is trustworthy is NOT achieved; P6/P2 stand but the residual's target-dependence remains UNEXPLAINED (an open question, honestly bounded). SCOPE: retrospective, in-silico meta-analysis, n=13, one cliff operationalization, correlation != causation; not wet-lab. Reads committed B54/B56 residuals; no src/ change. Reproduced x2 byte-identical (payload sha256 dc4c6654...). | B57_metrics.json |
| B58 (re-powered residual-mechanism test with ROGI — PARTIAL mechanism: roughness matters moderately; residual is multifactorial) | Re-tests B57's null with (a) a principled continuous multi-scale roughness index (ROGI, Aldeghi/Graff/Coley 2022 — REIMPLEMENTED dependency-free & validated: rough 0.42 > smooth 0.17) and (b) more targets (n=13->19). NOTE: `pip install rogi` broke the canonical env (numpy 1.26->2.4); RESTORED numpy 1.26.4, re-verified B57 byte-identical (dc4c6654), removed rogi/fastcluster, implemented ROGI myself. | 19 targets (LIT-PCBA 8 + TDC/Butkiewicz HTS 11; sarscov2_3clpro skipped <15 novel). Residual A1B1 reused from committed B54/B56 for 13; computed identically (property-matched decoys + NN<0.4 novel, Morgan HGB, 5 seeds) for 6 NEW targets. ROGI (1 - integral SD(t)dt/SD(0) on complete-linkage Tanimoto, binary label) + cliff density + diversity + n_actives computed uniformly. Spearman(residual, predictor). **Result (H1 FALSE, H2 TRUE): the residual is NOT cleanly predicted by any single property, but SAR ROUGHNESS is a genuine PARTIAL driver.** Ranked |Spearman|: assay_type_phenotypic -0.44 (only 2 phenotypic targets -> underpowered categorical), **ROGI -0.42** (right sign: rougher landscape -> lower irreducible signal; below the pre-registered -0.5 bar but clearly BEATS crude cliff density -0.22 -> H2 TRUE), diversity +0.31, n_actives +0.31, cliff -0.22. **Interpretation:** re-powering + a principled metric turned B57's flat null into a PARTIAL, multifactorial picture — roughness matters moderately (vindicating B57's self-critique that its null was partly crude-metric+low-power), the two phenotypic (cell-based antiviral) endpoints have the lowest residuals (suggestive, underpowered), and no single factor reaches |0.5| so NO clean predictive rule. **Candidate WORKING PRINCIPLE P7 (low confidence): the post-debiasing binding signal decreases with SAR-landscape roughness (real but weak/partial).** Methodological sub-finding: principled ROGI > threshold cliff density. **HONEST:** predictive-rule ambition NOT achieved; residual predominantly multifactorial/unresolved (likely needs 3D/pocket features, continuous potency, or larger n). SCOPE: retrospective, in-silico meta-analysis n=19, ROGI reimplemented (validated not bit-exact), correlation != causation; not wet-lab. Reads committed B54/B56 residuals; no src/ change. Reproduced x2 byte-identical (payload sha256 54865883...). | B58_metrics.json |
| B59 (does the residual differ by assay format? — NULL; bounds B58's phenotypic signal) | Confirmatory/post-hoc test (residuals already committed in B58): B58 flagged a suggestive assay-type signal driven by only 2 phenotypic targets; does it generalize to a powerable biochemical-vs-cell-based dichotomy? **Phase-2 finding: the PURE phenotypic test is NOT powerable (only 2 whole-cell antiviral VS datasets exist)**, so this is the closest powerable proxy. | 19 B58 targets classified by documented assay format: **biochemical (cell-free isolated protein/enzyme, n=9):** FEN1/MAPK1/ALDH1/GBA/KAT2A/PKM2/TDP1/VDR/ESR1; **cell-based (functional single-target + phenotypic, n=10):** m1(x2)/orexin1/kir2.1/kcnq2/cav3/choline/STK33 + hiv/sarscov2_vitro. Mann-Whitney U on the A1B1 residuals + rank-biserial effect. **Result (H1 FALSE — NULL): no difference.** Biochemical vs cell-based median residual **0.597 vs 0.646** (U=50, two-sided p=0.71, one-sided biochem>cell p=0.36, rank-biserial -0.11 — if anything cell-based is slightly HIGHER). Sensitivity excluding the 3 ambiguous targets (VDR/ESR1/STK33): still null (p=0.23). Phenotypic subgroup (n=2) residuals 0.45/0.57 = low but untestable. **Conclusion: B58's assay-type signal (Spearman -0.44) was driven by the 2 phenotypic antiviral points, NOT a general biochemical/cell-based effect; the phenotypic 'mechanism' is NOT supported at power; the pure phenotypic test is infeasible. The residual's target-dependence stays UNEXPLAINED (consistent w/ B57/B58).** This correctly BOUNDS B58's suggestive signal (prevents overclaiming a phenotypic mechanism). SCOPE: confirmatory/post-hoc (residuals known), assay classification external-fact-based but 3 targets ambiguous (sensitivity reported), n=19 modest power, association != causation; not wet-lab. Reads committed B58 residuals; no src/ change. Reproduced x2 byte-identical (payload sha256 2d7628c5...). | B59_metrics.json |
| B60 (continuous-potency roughness -> generalization: the INTERPOLATION/EXTRAPOLATION split — resolves the residual-mechanism arc) | The honest instrument for P7: measure roughness on REAL continuous potency (not binary labels) and test whether it predicts a ligand-based model's generalization. MoleculeACE (van Tilborg 2022, 30 curated ChEMBL targets, pKi/pIC50). Data downloaded directly (no pip -- B58 env lesson). | Per target: ROGI (continuous potency) + Morgan-1024 HGB REGRESSOR generalization = Spearman(pred,true) on NOVEL-chemistry (scaffold + NN<0.4) test vs RANDOM-split test, x3 seeds. 19/30 usable (11 skipped <15 novel test -- ChEMBL series often analog-dense, the B55 phenomenon). **Result (H1 FALSE, H2 FALSE-reversed): the KEY finding is a contrast.** ROGI predicts RANDOM-split (interpolation) generalization STRONGLY (Spearman **-0.55**, sig -- REPRODUCES the known ROGI<->modellability result) but NOVEL-chemistry (extrapolation) generalization only WEAKLY (**-0.33, p=0.17, ns** -- matching B58's binary -0.42). Extrapolation GAP is large: models interpolate ~0.8 but extrapolate to novel chemistry ~0.3 (several ~0). cliff-fraction vs novel-gen -0.41. **RESOLVES THE ARC (B57->B58->B59->B60): the doubly-debiased VS residual is an EXTRAPOLATION metric; roughness governs INTERPOLATION not EXTRAPOLATION, so B58's weak -0.42 was NOT a binary-label artifact -- continuous potency gives the SAME weak extrapolation answer. The persistent residual-mechanism nulls (B57-59) are EXPLAINED: extrapolation isn't governed by within-distribution roughness.** Theory: P7 refined (roughness->interpolation strong / ->extrapolation weak); killed the binary-label-attenuation hypothesis cleanly; NEW candidate P8 (moderate): the EXTRAPOLATION GAP -- ligand-based potency models interpolate well (~0.8) but extrapolate to novel chemistry poorly (~0.3), NOT governed by landscape roughness. SCOPE: retrospective, in-silico, n=19 (random-gen -0.55 sig; novel-gen ns), ROGI reimplemented (validated), random-split arm reproduces known work -- the interpolation/extrapolation CONTRAST + arc resolution are the new contribution; correlation != causation; not wet-lab. MoleculeACE in MANIFEST. Reproduced x2 byte-identical (payload sha256 bb2b03d3...). | B60_metrics.json |
| B61 (per-compound decomposition of novel-chemistry EXTRAPOLATION error — SURPRISE: AD-distance is NULL; error is regression-to-the-mean) | Attacks P8 via hypothesis-space construction: enumerate mechanism families, ELIMINATE using our evidence (B60 ruled out global roughness, B59 assay type, B32-48 representation), test the surviving PER-COMPOUND candidates simultaneously at high power. | 2,686 novel-chemistry (scaffold + NN<0.4) test compounds across 24 MoleculeACE ChEMBL targets; HGB potency regressor per target; per-compound |error| vs 4 competing predictors (Spearman, pooled + within-target) + a multivariate error-predictability model (leave-one-target-out OOF). **Result (H1 FALSE & OVERTURNED, H2 mechanical-true, H0 no): the canonical applicability-domain-distance heuristic does NOT predict extrapolation error (AD-distance Spearman -0.008, p=0.67 NULL; within-target +0.05). scaffold-novelty +0.02 (null), local-cliff +0.06 (tiny). The ONLY correlate is potency_shift = |true - train-median| (pooled +0.37 p~0, within-target +0.44) -- but it requires the LABEL and is largely MECHANICAL.** Interpretation (coherent): AD-distance null + error tracks label extremity => the model REGRESSES TO THE TRAINING MEAN on novel chemistry (no transferable signal -> defaults to prior), so chemical distance is irrelevant and error is just how extreme the true potency is. **Consequence (provocative, decision-relevant): the ubiquitous 'flag chemically-distant predictions' AD trust heuristic does NOT identify which novel-chemistry predictions are wrong; NO label-free structural feature does (all ~null). No cheap structural trust rule for potency extrapolation.** Theory: candidate P9 (moderate) -- novel-chemistry extrapolation error is governed by label shift/regression-to-mean, not covariate/chemical distance. Falsifies pre-registered H1 cleanly; mild tension w/ B30b (AD->error +0.128 aggregate ADMET) -> essentially absent for novel-chemistry POTENCY specifically. **HONEST CAVEAT:** potency_shift correlation is partly tautological (uses the label; diagnoses regression-to-mean) -- the ACTIONABLE result is the NULL on all label-free predictors; whether a calibrated CONFORMAL interval beats a point distance a priori is the natural next question (connects B30b). VALUE OF HYPOTHESIS-SPACE MODE: testing 4 mechanisms simultaneously at compound-level power surfaced the surprise a single-hypothesis run would have missed. SCOPE: retrospective, in-silico, n=2686 compounds/24 targets; pooling risks target-confound (within-target reported); correlation != causation; not wet-lab. Reproduced x2 byte-identical (payload sha256 e204be60...). | B61_metrics.json |
| B62 (shrinkage vs signal-loss decomposition of the extrapolation gap — SIGNAL-LOSS dominated; a genuine information ceiling, not a fixable calibration bug) | Phase-0 REJECTED the obvious conformal/ensemble-vs-AD experiment (would reproduce known UQ/AD/conformal-shift + regression-to-mean facts -- low info gain) and selected the discriminating question: is the novel-chemistry extrapolation gap correctable SHRINKAGE or fundamental SIGNAL-LOSS? | MoleculeACE 24 targets; HGB potency regressor, scaffold split, novel (NN<0.4) test; per target: rank_signal Spearman(pred,true), shrinkage_ratio std(pred)/std(true), correctable_fraction = squared-error share removable by ORACLE linear recalibration (upper bound on any monotone debiasing). **Result (H1 FALSE, H2 TRUE, strict-H0 not met -> signal-loss-DOMINATED):** median rank_signal **0.28** (weak ordering survives), median shrinkage_ratio **0.50** (predictions compressed to HALF the true spread, 100% of targets shrunk), median correctable_fraction **0.14** (only 14% of squared error recoverable even by oracle recalibration). Honest reading: the gap is NOT a fixable calibration problem (H1 optimistic hypothesis falsified) -- predictions ARE heavily+universally shrunk, but un-shrinking barely helps because the underlying ordering is too weak to recover; the shrinkage is a RATIONAL response to weak signal, not a correctable bias. **Bounds P8 pessimistically: novel-chemistry potency extrapolation is a genuine SIGNAL/INFORMATION ceiling** (refines P9: B61's regression-to-mean is a SYMPTOM of signal-loss). CLOSES the extrapolation-mechanism line (B60 roughness governs interpolation-not-extrapolation; B61 AD-distance null / regression-to-mean; B62 signal-loss not correctable): the only levers left are MORE/DIFFERENT information (new assays, 3D, acquisition near the query) -> points back to P4 data-acquisition, not any post-hoc modeling trick. INTEGRITY: pre-registered H3 (cross-dataset linkage to B58 residuals) was INFEASIBLE (disjoint target sets) -- caught+withdrawn pre-run, dated amendment, replaced by a descriptive within-B62 link (rank vs correctable Spearman -0.45). SCOPE: retrospective, in-silico, n=24; oracle recalibration uses test labels (upper bound, not deployable); can't fully separate signal-loss from assay noise; shrinkage/regression-dilution is textbook (novelty = the per-target novel-chemistry decomposition + pessimistic bound); not wet-lab. Reproduced x2 byte-identical (payload sha256 c74983c8...). | B62_metrics.json |
| B63 (does P6's bias independence/additivity survive a DIFFERENT decoy paradigm? — NO: P6 is HTS-paradigm-specific, an honest boundary on our most novel principle) | Close P6's one honest boundary: B54/B56 replicated P6 (analogue-bias ⊕ decoy-bias are INDEPENDENT/ADDITIVE, interaction≈0) but ONLY under the HTS decoy paradigm (decoys=assay inactives); B55 couldn't test the DUD-E paradigm (real DUD-E actives too analogue-clustered to populate a novel arm). B63 CONSTRUCTS the missing instrument — diverse MoleculeACE/ChEMBL actives + DUD-E-style decoys (property-matched in z-scored 6-D physchem AND topology-dissimilar, max-Tanimoto<0.5, greedy no-replacement 3:1) from an 8,000-compound ChEMBL background — and re-runs the B54/B56 2×2 factorial. | 2×2 factorial {A0 random-bg vs A1 DUD-E-matched decoys} × {B0 random split vs B1 novel-chemistry (scaffold-disjoint + NN<0.4)}, Morgan-1024→HGB, 3 seeds, 14 usable targets (10 skipped <15 novel test actives — the B55 analogue-density phenomenon). **Result (H1 FALSE, H2 TRUE):** panel interaction **−0.0553** (|·|≥0.03), vs HTS-paradigm B54 −0.019 / B56 −0.0005 (≈0). Cells: A0B0 0.963 (both biases), A1B0 0.938 (decoy-controlled), A0B1 0.843 (analogue-controlled), **A1B1 0.763 (doubly-controlled honest signal)**; decoy-effect +0.053, analogue-effect +0.148. **The decoy effect is LARGER in the novel-chemistry regime (0.080) than the standard regime (0.025)** → the two biases are sub-additively ENTANGLED under DUD-E decoys. Mechanism (coherent): DUD-E decoys are topology-dissimilar BY CONSTRUCTION, so once actives are also forced novel, property-matching does more separating work — an entanglement absent for HTS assay-inactive decoys (B54/B56), where the two controls stay independent. **Theory: P6 DEMOTES from candidate general law → HTS-paradigm-specific principle; under constructed DUD-E-style decoys honest evaluation needs BOTH controls AND their interaction term, not a simple additive subtraction.** A strong doubly-controlled signal still survives (A1B1 0.763). INTEGRITY: the first execution had a decoy-builder bug (over-generated ~all-background decoys instead of 3:1, confounding the A0-vs-A1 contrast, and was pathologically slow — profiled via `sample`); caught pre-result via slowness investigation, fixed to the pre-registered 3:1 round-robin design, dated prereg amendment; NOTHING was committed before the fix so nothing is retracted. SCOPE: retrospective, in-silico, constructed decoys (our faithful DUD-E recipe, documented), MoleculeACE potent ChEMBL actives as the active class, 6-descriptor matching, n=14 targets; whether the negative interaction is mechanistic vs a construction artifact (topology-ref of first-50 actives, TOPO_MAX=0.5) is the natural follow-up; not wet-lab. Reproduced x2 byte-identical (payload sha256 07372f1a...). | B63_metrics.json |
| B64 (END-TO-END PLATFORM DEMONSTRATION on FEN1 — the whole pipeline composed on one disease target; a capability demo, NOT a hypothesis test) | Turn the validated modules into a usable closed-loop discovery engine and demonstrate it end-to-end on a real disease (Prasad's directive: build + demonstrate the platform). Target = **FEN1** (flap endonuclease 1; DNA-repair / synthetic-lethality oncology), chosen for HONESTY not cherry-picking — it has our STRONGEST doubly-controlled ligand signal (B45 novel-chem residual ≈0.80). | Pipeline composed (all previously-validated modules, strictly reused): FEN1 activity QSAR (LIT-PCBA, 360 actives + 10k inactives, Morgan-1024→HGB, Tanimoto AD + Mondrian conformal) → target-conditioned BRICS-GA generation (objective F = QED×synth×safety×P(FEN1-active), pop 60 / 8 gens, seeded ChEMBL seeds) → multi-channel scoring (activity + conformal + AD; safety=1−mean(hERG,AMES,DILI); synth solvable-prob; QED; SA) → ranked top-20 shortlist, each annotated reliable(in-domain) vs low-confidence(OOD) + nearest-known-active Tanimoto. **QSAR validated up front: random held-out AUROC 0.941, scaffold-disjoint 0.960** (novel-chem ≈0.80 from B45). Generation measurably optimized the objective (best 0.14→0.31, mean pop 0.003→~0.05 over 8 gens). **Result: 20 candidates, only 3 flagged RELIABLE (in-domain); 19 are novel-chemistry flagged "OOD — do not trust the activity call".** The high-p_active (0.90+) candidates are almost all OOD with mostly-empty conformal sets `{}` (model declines to commit); the reliable in-domain hits sit near known chemistry (NN-Tanimoto ~0.34–0.42; top = protocatechuic-acid-like O=C(O)c1ccc(O)c(O)c1, p_active 0.785, low hERG/AMES/DILI). **The demonstration's value IS the honesty: the platform generates AND reports what it cannot trust — a direct product of P2 (similarity-inflation) + P9/B60–B62 (novel-chemistry extrapolation is an information ceiling); it flags rather than false-positives the OOD 0.90+ molecules.** SCOPE: capability demonstration, NOT a hypothesis test; output = ranked COMPUTATIONAL HYPOTHESES, NOT validated actives / NOT drugs; retrospective in-silico, open data; p_target_active is a QSAR probability not measured activity; generated mols are BRICS recombinations of known chemistry; only in-domain predictions trustworthy; no wet-lab, no prospective confirmation — the drug still needs wet-lab (resource-gated, human decision). Deterministic; Reproduced x2 byte-identical (payload sha256 739f5230...). Write-up: experiments/B64_endtoend_fen1_demonstration/DEMONSTRATION.md + shortlist CSV. | B64_metrics.json |
| B65 (does active learning's label-efficiency advantage COLLAPSE in the novel-chemistry regime? — FLAT NULL: acquisition strategy is not the lever in the weak-signal small-label regime) | Ladder rung (VISION development-path step 4 "fewer labels" / information-removal): the canonical "AL beats random" is solved (Reker&Schneider; Deep Docking), so the only novel question our P9/B62 information-ceiling motivates is whether AL's advantage survives on NOVEL chemistry (NN<0.4) — a question the literature (confirmed by 3 research threads) has NOT answered for potency. | Simulated pool-based AL on MoleculeACE continuous potency (24 targets): query-by-bagging ensemble UNCERTAINTY sampling AND GREEDY vs RANDOM; learning curves (Spearman-vs-#labels AUC) measured separately on an in-domain (interpolation) test AND a scaffold-disjoint+NN<0.4 novel-chemistry (extrapolation) test; 3 seeds; mechanism probe = acquired-compound similarity to labeled set. **Result (H1 FALSE, H2 UNSUPPORTED, H0 not real — FLAT NULL):** neither uncertainty nor greedy AL reliably beats random on EITHER test — median adv_indomain(unc) **−0.013** (frac_pos 0.33), adv_novel(unc) **+0.017** (frac_pos 0.54); all within noise (|median|<0.03, sign ~50/50). The acquisition functions demonstrably DID select the intended compounds (acq_sim: uncertainty 0.460 < random 0.500 < greedy 0.565 — uncertainty explores, greedy densifies), so correct acquisition simply did NOT translate into better generalization. **My pre-registered H2 ("advantage collapses specifically on novel chemistry") was wrong in SHAPE — there was no in-domain advantage to collapse FROM; the honest result is a flatter null.** INTEGRITY: the auto-verdict initially over-read this as "H0 hopeful — AL survives on novel chemistry" (bare 0.01 threshold on a noisy median); CORRECTED pre-commit to require |median|≥0.03 AND a target-majority sign before crediting an effect — verdict is excluded from the payload so numbers are unchanged. HONEST CAVEAT (foregrounded): H1 was a SANITY CHECK (AL should beat random in-domain, reproducing canon) and it FAILED — most likely the small-label (20→140)/small-ensemble/continuous-potency setup is UNDERPOWERED vs the large-library CLASSIFICATION hit-finding regime where AL is genuinely established; so B65 does NOT contradict canonical AL — it is scoped to the weak-signal small-label potency regime, where the answer is "acquisition strategy is not the lever." Theory: **P4 read honestly = the AMOUNT/quality of information dominates, NOT smart acquisition STRATEGY** (the latter unsupported here); REINFORCES B62 (when signal is weak, WHICH points you label barely matters). Ladder value: eliminates the assumption that clever acquisition can cheaply buy novel-chemistry generalization → the wet-lab-minimization lever is information, not selection. Natural follow-up (logged): AL in the large-library classification hit-finding regime (LIT-PCBA/Butkiewicz) to locate where AL's advantage turns on. SCOPE: retrospective, in-silico, MoleculeACE, one uncertainty proxy (bagging), simulated pool (optimistic); n=24; correlation≠causation; not wet-lab. Reproduced x2 byte-identical (payload sha256 f66986ab...). | B65_metrics.json |
| C1 (BACK-HALF proving-ground — zero-data structure-based binding on SARS-CoV-2 Mpro; WEAK-BUT-REAL signal) | First proving-ground chapter of the reoriented (zero-data) vision: can label-free structure-based reasoning recover real Mpro binders with ZERO target activity data? Tests the binding-reasoning capability (#5) on a well-understood target with held-out ground truth. | AutoDock Vina docking of 390 compounds (78 held-out crystallographic actives from TDC sarscov2_3clpro_diamond + 312 seeded inactives) into the Mpro 6LU7 active site; ZERO Mpro labels used; RDKit ETKDG 3D → obabel pdbqt → Vina (seed 42). Arm A = pure Vina rank; Arm B = + physical-validity (protein-clash) gate. **Result (H1 TRUE but WEAK, H2 false):** actives dock significantly better than inactives (Mann-Whitney p=0.0001; median −5.91 vs −5.53) → docking carries REAL zero-data signal; but enrichment is WEAK — AUROC 0.635, EF1% 1.25, BEDROC20 0.27 (near-random early enrichment, consistent w/ Thread A's prospective-docking finding). Validity gate non-discriminating (all 383 docked poses passed the 2.0Å clash check — fragments small; gate needs strain/interaction terms). Honest read: the capability EXISTS but is not trustworthy at the top of a screen; motivates homology-transfer + physics-gating. SCOPE: retrospective (held-out answer key), crystallographic FRAGMENT ground truth (deliberately hard), single receptor, Vina only, no induced fit; output = pose-plausible hypotheses NOT potency; not wet-lab. Reproduced x2 byte-identical (payload sha256 a5c4e13b...). | C1_metrics.json |
| TID1 (FRONT-HALF chapter 1 — zero-data target identification by leave-organism-out homology transfer; HONEST BOUNDARY: homology-only is dominated by generic conservation globally, but adds precision at the top + abstains well) | First FRONT-HALF capability of the reoriented vision (disease→target), zero-data-native (pathogen proteome only, NO disease-specific data), built as first adapters on a minimal LIVING provenance/confidence-tiered substrate. Chosen from the 4-paradigm front-half investigation (comparative-genomics homology = the zero-data-native lever). Feasibility-driven amendment: OGEE/DEG essentiality unreachable → primary signal pivoted to DRUGGABILITY transfer w/ UniProt ChEMBL-xref ground truth (documented, PRE-BUILD). | Panel = 4 pathogens (M.tuberculosis, E.coli, P.aeruginosa, P.falciparum; full UniProt reference proteomes), leave-organism-out: for held-out X, transfer druggability via mmseqs2 homology of X's proteome to the OTHER organisms' ChEMBL-xref drug targets; human-subtraction (Adapter-S) for selectivity; abstain where no target homolog. Recovery of X's own ChEMBL targets vs THREE nulls incl. a CONSERVATION null (homology to other orgs' FULL proteomes) — the key non-triviality control. **Result (H1 FALSE, H3 FALSE, H4 TRUE — first-class honest boundary):** panel-median druggability AUROC **0.641** does NOT beat the **conservation null 0.721** (bacterial/parasite drug targets are largely just well-conserved proteins → homology-to-targets ≈ generic conservation, GLOBALLY). **BUT precision@k is ~2× (0.192 vs 0.093 conservation, all 4 orgs)** — target-specific homology adds real precision at the TOP of the ranking where candidates are picked. **Abstention is CALIBRATED (H4):** ~92% of proteins (no homolog) correctly flagged low-confidence; the ~8% committed-to are enriched for true targets (knows what it doesn't know). **Naive human-subtraction HURT (H3 false, armB 0.538<<0.641)** — many bacterial targets are conserved WITH human homologs, so binary subtraction removed true positives; selectivity needs a differential (not binary) treatment. **Eliminates "homology transfer alone suffices" and points precisely at the next capabilities (structure/pocket, mechanistic/essentiality) to go BEYOND conservation** — exactly what Q5/H0 anticipated. LIVING SUBSTRATE demonstrated: outputs are provenance/confidence-tiered records (query protein, best homolog, source organism, %id, tier); self-generated records quarantined at low tier (guardrail). ENV: fresh `bioinfo` env (mmseqs2 v18, isolated — the numpy/rogi lesson). SCOPE: retrospective recovery of KNOWN targets (proving ground), reviewed-proteome-derived ground truth (ChEMBL xref); reference proteomes (ORF-calling upstream, mature); essentiality (OGEE/DEG) deferred (unreachable) as a future adapter; SARS-CoV-2 excluded from powered stats (n=3); orphan/dark proteins quarantined; not wet-lab. Data: UniProt reference proteomes + ChEMBL-xref target lists (PUBLIC/OPEN, MANIFEST). Reproduced x2 byte-identical (payload sha256 e71fccb8...). | TID1_metrics.json |
| TID2 (FRONT-HALF chapter 2 — does INTRINSIC structural pocket druggability beat/add-beyond sequence conservation for zero-data target-ID? NO: near-random alone, adds only a whisper — a well-controlled BOUNDARY) | New ADAPTER on the TID1 living substrate (extensibility in action). TID1 showed homology-to-targets ≈ generic conservation; the 4-paradigm "beyond-conservation" investigation found the one genuinely ORTHOGONAL, feasible signal is intrinsic pocket geometry (druggability scored on the protein's OWN structure, conservation-free by construction). | Same 4-pathogen leave-organism-out proving ground; per protein: AlphaFold DB v6 structure → fpocket max Druggability Score (conservation-free); vs a mmseqs2 CONSERVATION null (homology to other orgs' full proteomes). Eval set = all 411 ChEMBL-xref targets + seeded ~400 non-targets/org (2011 proteins, 1990 with AF structures; fpocket cached as a deterministic data artifact). DECISIVE test = held-out logistic `is_target ~ conservation + structural`, nested ΔAUROC + partial coefficient. **Result (H1 FALSE, H2 FALSE/MARGINAL — first-class boundary):** structural druggability ALONE AUROC **0.543** (near-random) vs conservation null **0.729**; precision@k 0.23 vs 0.46 (structure far WORSE at the top). After partialling out conservation, structure adds only a **whisper** — held-out ΔAUROC **+0.005** (3/4 orgs positive, 1 negative), partial coef **+0.066 vs conservation +0.700** (~10:1). fpocket's known LOW SPECIFICITY (calls too much of the proteome druggable — Thread-1 caution) makes it a poor target discriminator. INTEGRITY: auto-verdict initially credited "H2 TRUE" on a bare dl>0 & cs>0 threshold; CORRECTED pre-commit to require a MEANINGFUL effect (ΔAUROC≥0.02 AND ≥3/4 orgs AND coef≥0.15) — the B65 over-reading lesson. **ARC CONCLUSION (TID1+TID2): for zero-data pathogen target-ID, NEITHER target-specific homology NOR structural pocket druggability meaningfully beats generic CONSERVATION (~0.73 AUROC) — conservation is the robust workhorse signal; the practical recipe is rank-by-conservation + host-nonhomology selectivity + abstain. Next lever (mechanistic essentiality/chokepoint) is itself mostly-conservation + fragile (investigation Thread 2) → we are near an honest ceiling for BEATING conservation on this task.** SCOPE: retrospective known-target recovery; fpocket crystal-trained model on apo AF models; static structures miss ~40% cryptic/allosteric pockets (systematic bias, stated); leave-organism-out (not leave-superfamily-out — noted); eval-set sample not full proteome; not wet-lab. Data: AlphaFold DB v6 structures + fpocket cache (PUBLIC/OPEN, MANIFEST). Env: bioinfo (fpocket 4.2.3 + mmseqs2 18). Reproduced x2 byte-identical (payload sha256 62efe740...). | TID2_metrics.json |
| E2E1 (END-TO-END composition — the validated front+back capabilities wired into ONE zero-data pipeline on a pathogen; proteome→target→pocket→docked→ranked shortlist) | First composition of the reoriented vision's pieces: does the whole zero-data pipeline run end-to-end from a pathogen PROTEOME and emit a coherent, honestly-tiered candidate shortlist? Capability-composition DEMO (not a discovery claim). Pathogen = M. tuberculosis (TB), standing in for a novel pathogen (ZERO TB activity data used). | Pipeline: target-ID (conservation×fpocket-druggability×host-nonhomology, leave-Mtb-out) → pick top-composite known druggable target → its best fpocket pocket becomes the Vina box (front→back bridge) → dock a 120-cmpd ChEMBL screening library (C1-style Vina, seed=42) → + ADMET(hERG/AMES/DILI) + synth → ranked provenance/confidence-tiered shortlist. **Result (composition WORKS, honest):** ran end-to-end (531 Mtb proteins, 131 known targets; chose P9WGY9; fpocket pocket found; 114/120 docked; ADMET/synth applied; top-20 tiered shortlist). Self-reports every prereg caveat: target-ID composite precision@k **0.305 < conservation 0.382** (composite does NOT beat conservation — the TID1/TID2 boundary, not spun; adding druggability + human-subtraction doesn't help). Top candidate CCN(CC1CCOCC1)C(=O)Cn1cccc(C(F)(F)F)c1=O (Vina −6.01, safety 0.70, QED 0.82, synth 0.91, in-domain) flagged "reliable-ADMET (docking POSE-PLAUSIBLE HYPOTHESIS)" — NOT hit/drug. **Value = demonstrating the validated pieces COMPOSE end-to-end from a proteome on the living substrate (the vision's shape at small scale), with every limitation stated.** SCOPE: composition demo, output = ranked COMPUTATIONAL HYPOTHESES (not validated hits/drugs); target-ID conservation-dominated; docking weak at the top (C1); ADMET/synth are disease-agnostic transfers; generic screening library (not curated TB actives); zero TB activity data; no wet-lab. Envs: bioinfo(mmseqs/fpocket)+docking(vina/obabel)+intercepta-build(admet/synth). Reproduced x2 byte-identical (payload sha256 9ee75a21...). | E2E1_metrics.json |
| SIL1 (SELF-IMPROVING LOOP chapter 1 — the conformal-gated living-net mechanism + its anti-self-deception guardrail, demonstrated rigorously; a MODEST-BUT-CONSISTENT, well-controlled POSITIVE) | First rigorous test of VISION principle 6 (the system is LIVING — its own validated findings feed back to improve the task — with the guardrail that self-generated knowledge is confidence-tiered/quarantined or the loop becomes self-DECEPTION). Chosen autonomously over GNINA-rescoring (modest/incremental) + breadth (lower-novelty) + cancer→antimicrobial synergy (data-starved) after a 3-thread Phase-0; Thread-2 gave the exact rigorous recipe (with/without ablation + shuffled/ungated negative controls + leakage control; only conformally-validated outputs feed back — anti model-collapse, Shumailov Nature 2024). | 6 TDC ADMET classification tasks (ames/cyp2d6/bbb_martins/herg/bioavailability_ma/dili), scaffold-disjoint TRAIN(≤250)/UNLABELED-pool/TEST, 3 seeds. 4 arms on the SAME held-out TEST: **A** train-only · **B** train + conformally-CONFIDENT SINGLETON pseudo-labels (GOLD) · **C** ungated (train+ALL pool argmax pseudo-labels) · **D** B's compounds w/ SHUFFLED labels. Model = admet _TaskModel (HGB+Mondrian conformal, B30b-calibrated). **Result (H1 TRUE, H2 TRUE, H3 TRUE):** the loop HELPS — median ΔB−A **+0.011 AUROC, ALL 6/6 tasks positive** (modest but consistent); the GATING is the guardrail — median ΔB−C **+0.009** (gated beats ungated) and ΔB−D **+0.043** (correct beats SHUFFLED: fake knowledge clearly degrades the model); and conformal genuinely identifies trustworthy self-knowledge — GOLD pseudo-label accuracy **0.886 vs full-pool 0.772**. **Principle 6's living-net mechanism AND its anti-self-deception guardrail are demonstrated rigorously: feeding back the system's OWN conformally-validated findings modestly-but-consistently improves the task, while ungated/shuffled feedback does NOT (and shuffled hurts).** INTEGRITY: unlike B65/TID2 this positive was NOT over-read — 6/6 tasks + decisive negative controls make it real (not a whisper); framed honestly as MODEST magnitude. Design note: an early smoke test showed the loop HURTING under an un-capped 1414-cmpd train; the small-train cap (room for the loop) fixed it — documented. Contribution = NOT inventing self-training (established) but rigorously validating the living-net MECHANISM + confidence-gating GUARDRAIL tied to calibrated conformal prediction, with the negative controls most demos omit. SCOPE: retrospective, in-silico, 6 TDC tasks; within-task loop (findings-while-working); cross-task accumulation (findings-help-FUTURE-problems) is the harder likely-negative-transfer follow-up (logged); modest effect; not wet-lab. Reproduced x2 byte-identical (payload sha256 97a4a54c...). | SIL1_metrics.json |
| SIL2 (SELF-IMPROVING LOOP chapter 2 — does the SIL1 loop survive DISTRIBUTION SHIFT? NO: it's a NEAR-DOMAIN phenomenon — maps where the living-net loop works vs breaks; connects principle 6 to the B62 ceiling) | SIL1 showed the conformal-gated loop helps IN-DOMAIN; the vision's stress regime is NOVEL chemistry (a new pathogen's compounds are unlike training). Autonomous choice after GNINA (Thread-1's back-half lever) proved INFEASIBLE on this Apple-Silicon Mac (Linux+CUDA-only) and cross-task accumulation proved ill-posed (activity labels not transferable across targets). | SIL1's 4-arm ablation (A train-only, B train+conformal-singleton GOLD pseudo-labels, C ungated, D shuffled) re-run on 17 MoleculeACE targets (binarised potency), scaffold-disjoint carve, evaluated on BOTH an IN-DOMAIN test AND a NOVEL-chemistry test (scaffold-disjoint + NN-Tanimoto<0.4 to train); 3 seeds. **Result (H1 TRUE, H2a FALSE → H2b: NEAR-DOMAIN ONLY):** in-domain the loop helps modestly (median ΔB−A **+0.008, 76.5% of targets positive** — replicates SIL1 directionally, weaker because MoleculeACE is analog-dense = strong base models / less room); on NOVEL chemistry the benefit is a **WASH** — median ΔB−A +0.007 but only **52.9% of targets positive (a COIN-FLIP)** → NOT a reliable effect. So **self-accumulated IN-DOMAIN knowledge does NOT reliably cross the novel-chemistry ceiling — the living-net loop is bounded to the near-domain regime** (consistent with B62: novel-chemistry potency is an information ceiling). The GUARDRAIL still holds (shuffled hurts: in-domain median ΔB−D +0.101, novel +0.021; gold pseudo-label acc 0.858). INTEGRITY: auto-verdict over-read "H2a survives shift" on the bare median (+0.007>0.005); CORRECTED pre-commit to require median>0.005 AND target-majority ≥60% positive before crediting an effect — the B65/TID2 coin-flip lesson (novel frac 0.529 fails). **ARC (SIL1+SIL2): principle 6's living-net loop genuinely helps + is guarded WHERE the model has signal/room (in-domain), but its benefit does NOT reliably extend to novel chemistry — the honest scope of the self-improving loop.** SCOPE: retrospective, in-silico, MoleculeACE, binarised potency, within-task loop, in-domain pool (GOLD knowledge is in-domain by construction), n=17; modest effects; not wet-lab. Reproduced x2 byte-identical (payload sha256 a2072419...). | SIL2_metrics.json |
| TID3 (BREADTH — does zero-data target-ID generalize across KINGDOMS? DEGRADES monotonically w/ phylogenetic distance, and abstention does NOT track it — a silent-failure boundary) | Development-ladder breadth step: stress-test the TID1/TID2 conservation-ceiling across disease CLASSES. TID1/TID2 were 4 bacteria/parasite; TID3 spans 3 KINGDOMS. | TID1's leave-organism-out druggability-transfer (mmseqs2 homology to OTHER orgs' ChEMBL-xref targets) vs conservation null, over a 7-organism panel: bacteria(mtb/ecoli/paeruginosa) + protozoan parasites(pfalciparum/tbrucei/lmajor) + fungus(calbicans); viruses EXCLUDED (species proteomes strain-fragmented/near-empty), K.pneumoniae dropped (strain-mismatch, 0 in-proteome targets). **Result (H1 weak, H2 TRUE, H3 FALSE):** zero-data target-ID DEGRADES MONOTONICALLY across kingdoms — precision@k(druggability) **bacteria 0.236 > parasite 0.083 > fungus 0.000**; the kingdom-ISOLATED fungus (no same-kingdom reference) recovers ZERO of its 19 targets at top-k → homology transfer weakens with phylogenetic distance (cross-kingdom transfer fails). The conservation ceiling PERSISTS per-kingdom (druggability AUROC ≈/< conservation-null 0.68–0.85 everywhere — TID1 holds across classes). **CRITICAL boundary (H3 FALSE):** abstention does NOT track the degradation — the fungus abstains at ~the SAME rate (0.933 vs 0.929 same-kingdom) yet recovers nothing → it is CONFIDENTLY WRONG on a distant kingdom. So TID1's 'calibrated abstention' does NOT extend cross-kingdom: **the system SILENTLY FAILS on phylogenetically-isolated pathogens (doesn't know it doesn't know)** — a real vision-relevant limitation for truly-novel pathogens. INTEGRITY: auto-verdict falsely said the fungus 'abstains more' (0.933≈0.929 = equal); CORRECTED pre-commit to the honest opposite (abstention miscalibrated cross-kingdom, H3 FALSE). SCOPE: retrospective known-target recovery; full reference proteomes; leave-organism-out; small target-sets for new classes (12–19 → modest power, n=1 fungus); viruses infeasible (stated); breadth/robustness result, not a new capability; not wet-lab. Data: UniProt (MANIFEST). Reproduced x2 byte-identical (payload sha256 8ca4029f...). | TID3_metrics.json |
| TID4 (fix the silent failure? — can a LABEL-FREE signal predict when zero-data target-ID fails? NO — first-class negative: the silent failure is NOT cheaply fixable) | TID3 exposed that zero-data target-ID SILENTLY fails on phylogenetically-isolated pathogens (per-protein abstention doesn't flag organism-level failure). TID4 asks: is there a label-free, query-time signal (computable for a brand-new organism, NO target labels) that predicts whether target-ID will work → organism-level abstention? Powered as the data allows (user directive: expand panel + build properly). | Expanded to an 11-organism, 3-kingdom panel (7 bacteria + 2 parasites + 2 fungi) via UniProt REVIEWED proteomes + ChEMBL-xref targets by organism_id (consistent, targets⊆proteome — avoids TID3's strain mismatch). DATA LIMIT (honest): ChEMBL drug-target ground truth is sparse/strain-fragmented → most pathogens have <5 in-proteome targets or tiny proteomes; a large powered panel is NOT available (n=11, bacteria-weighted, stated). Per held-out organism: druggability-transfer recovery (precision@k) + 4 LABEL-FREE confidence signals from the organism's proteome vs the reference proteomes ONLY (S1 median homology-bits, S2 fraction-with-homolog, S3 same-kingdom-ref count, S4 90th-pct bits). Test: Spearman(signal, recovery) across organisms + organism-level abstention AUROC. **Result (H1 FALSE / H0 first-class):** NO label-free signal predicts organism-level recovery — best S1 Spearman **−0.31** (all |r|<0.5, and weakly NEGATIVE/counterintuitive), abstention AUROC **0.38 (worse than random)**. Per-organism shows why: well-connected bacteria (H.pylori S1=140, K.pneumoniae S1=164) recover ZERO targets while a poorly-connected parasite (T.cruzi S1=0) recovers 0.5 — homological distance simply does NOT tell you if target-ID will work. **The silent-failure problem TID3 exposed is NOT cheaply fixable: the system genuinely CANNOT tell from sequence homology alone when it is out of its depth on a new pathogen — a real limitation for the zero-data vision (deploy on a novel pathogen not knowing if target-ID is trustworthy).** More informative than a tidy positive. INTEGRITY: honest H0 (no over-read; the negative is reported plainly). SCOPE: retrospective; reviewed proteomes (enriched, but consistent within TID4 — the correlation is the test, not absolute level); n=11 bacteria-weighted (data-imposed, stated); organism-level recovery likely driven by target-class/annotation factors not captured by homology; not wet-lab. Data: UniProt reviewed (MANIFEST). Reproduced x2 byte-identical (payload sha256 e7d00a2a...). | TID4_metrics.json |
| MET1 (MECHANISTIC target-ID — FBA gene-essentiality is the FIRST orthogonal signal to BREAK the conservation ceiling, for metabolic targets in E. coli) | TID1–TID4 established that every SEQUENCE-based target-ID signal (homology/structure) ≈ generic CONSERVATION — a robust ceiling. FBA gene-essentiality is fundamentally different: from an organism's own genome-scale metabolic model, 'is this gene's deletion LETHAL in its reaction network?' — a mechanistic, NON-homology signal computable per-organism from sequence (works for a novel pathogen, zero activity data). Chosen autonomously after a Phase-0 ranked it top by value×CPU-feasibility×validatability. | COBRApy 0.31.1 + GLPK on arm64 (no GPU; verified): BiGG iML1515 (E. coli) → single-gene-deletion → FBA-essential (KO growth <1% WT). Gene-level test on the METABOLIC subproteome: per GEM gene (mapped via BiGG's UniProt xref, 1515/1516) — FBA-essential + is_drug_target (ChEMBL-xref) + conservation (mmseqs2 homology to OTHER orgs' targets, leave-E.coli-out). **Result (H1 TRUE, H2 TRUE — CEILING BROKEN within scope):** 1514 genes, 195 essential, 101 in-GEM drug targets. H1 enrichment: FBA-essential genes are drug targets at **25.6% vs 3.9%** non-essential — **odds ratio 8.6**; essentiality alone AUROC **0.696 > conservation 0.609**. H2 DECISIVE (beyond conservation): **5-fold-CV ΔAUROC +0.132** (cons 0.61 → cons+ess 0.74), essentiality partial coef **+0.71 vs conservation +0.35** (TWICE the weight). A LARGE, cross-validated, well-controlled additive lift — the H2 test partials out conservation so it's NOT the 'essential genes are conserved' circularity. **The FIRST orthogonal, non-homology signal that adds target-ID information beyond the conservation ceiling — the positive payoff after 4 boundary chapters.** INTEGRITY: genuine strong positive (CV'd ΔAUROC 0.132, OR 8.6) — NOT over-read (unlike B65/TID2/SIL2/TID4 auto-verdict corrections); scoped honestly. SCOPE (foregrounded): SINGLE organism (E. coli — the only BiGG GEM with UniProt gene mapping; S.aureus/K.pneumoniae GEMs lacked it → generalization UNTESTED); METABOLIC subproteome ONLY (FBA blind to the 81/182 non-metabolic drug targets); default growth medium (essentiality is medium-dependent); FBA essentiality has known modest precision (used here as a predictive FEATURE, which works); gene-level n=1514 (well-powered) but organism n=1; not wet-lab. Data: BiGG + UniProt (MANIFEST). Reproduced x2 byte-identical (payload sha256 5b9f9b48...). | MET1_metrics.json |
| MET2 (does MET1's mechanistic ceiling-break GENERALIZE across bacteria? REPLICATES in the 2 testable bacteria; broad generalization DATA-LIMITED — the honest, panel-tempered answer) | MET1 was single-organism. MET2 generalizes it the clean way (Prasad's directive: build GEMs DE NOVO from each UniProt proteome via CarveMe → genes = UniProt accessions by construction, sidestepping the BiGG/strain/locus-tag wall). Feasibility verified: CarveMe 1.6.6 + SCIP (free MILP) + diamond 2.2.4 all run on arm64 (no GPU); carve ~10-25s/organism. | 7-bacteria panel (E.coli/M.tuberculosis/P.aeruginosa/B.subtilis/H.pylori/Salmonella/E.faecalis), de-novo CarveMe GEMs, DEFAULT/complete medium (glucose-minimal infeasible — Mtb doesn't grow; documented), COBRApy single-gene-deletion → FBA-essential (UniProt-keyed); is_drug_target (ChEMBL-xref); conservation (mmseqs2). MET1's gene-level H1/H2 per-organism + pooled. **Result (REPLICATES where testable; broad generalization DATA-LIMITED):** H1 essential↔drug-target ENRICHMENT is broad — odds ratio 5.8-18.5 in all 3 orgs with ≥15 targets (E.coli 18.5/Mtb 6.0/Pae 5.8) — the essential↔druggable link is real + general. H2 (adds BEYOND conservation) is reliably testable ONLY in E.coli (94 targets) + Mtb (67) — the only bacteria with enough drug targets for a stable 5-fold-CV estimate — and in BOTH it replicates MET1 (CV ΔAUROC **+0.053, +0.040**; pooled +0.040). Every other bacterium has ≤20 in-GEM targets → its ΔAUROC is too noisy to interpret (Pae +0.014, Bsub −0.125 within noise). **So MET1's ceiling-break REPLICATES in the 2 reliably-testable bacteria (genuine) but broad bacterial generalization is NOT ESTABLISHED nor disproven — limited by drug-target ground-truth SPARSITY, not signal failure.** INTEGRITY: the initial 3-organism run over-claimed 'generalizes'; the EXPANDED 7-bacteria panel + a reliable-power threshold (≥50 targets for a CV estimate) tempered it to this honest data-limited conclusion — the discipline (more data → honester answer) working. SCOPE: de-novo default-medium GEMs (fewer core-essentials, weaker than curated iML1515); metabolic subproteome only; drug-target sparsity is the binding limit; not wet-lab. Reproduced x2 byte-identical (payload sha256 a9ccd34b...). | MET2_metrics.json |
| MET3 (CAPSTONE of the MET line — does the MET essentiality signal translate from a logistic COEFFICIENT into a better RANKED target shortlist? the practical front-half payoff) | MET1/MET2 proved essentiality adds beyond conservation via CV ΔAUROC. But the vision pipeline consumes a RANKED shortlist, not a coefficient — so the operative question is whether that global-AUROC gain reaches the TOP of the list (precision@k), where target-ID decisions are actually made. Re-uses MET2's per-gene conservation + FBA-essentiality (no new data); adds held-out ranking metrics. | E.coli + M.tuberculosis (the 2 reliably-testable bacteria from MET2). Per GEM gene: conservation (mmseqs2 to other-org targets) + FBA-essentiality (MET2 cache) → composite = **5-fold out-of-fold** logistic P(target|cons,ess) (held-out, no overfitting); baseline = conservation alone. Metrics: precision@k (k=#targets), enrichment@k, AUROC. **Result (H1 PARTIAL/DIRECTIONAL — improves in BOTH but magnitudes differ sharply):** E.coli SUBSTANTIAL — P@k **0.277→0.351** (ΔP@k +0.074), enrichment **4.91→6.23×** over prevalence, AUROC 0.633→0.677; top-10 composite recovers real essential-metabolic druggable targets (AcrB P31224, IspF P62617, MurG P0A6W3). Mtb MARGINAL — P@k **0.239→0.254** (ΔP@k +0.015 ≈ ONE extra target), AUROC 0.631→0.650: the top-k gain is SMALLER than Mtb's MET2 ΔAUROC, i.e. the global-AUROC improvement does NOT fully reach the TOP of the Mtb list. **Honest reading: the MET essentiality signal IS a practical ranked-shortlist (front-half) improvement — clearly in E.coli, weakly in Mtb — direction robust, magnitude organism-dependent (consistent with MET2).** INTEGRITY: initial auto-verdict said 'improves top-k in BOTH' (literally true but glossed Mtb's whisper); tempered PRE-COMMIT to PARTIAL/DIRECTIONAL foregrounding the E.coli-vs-Mtb magnitude gap. NOT a new discovery — the practical FORM of MET1/MET2; the MET line's capstone. SCOPE: metabolic subproteome; 2 bacteria; OOF composite (no overfitting); not wet-lab. Reproduced x2 byte-identical (payload sha256 a6b15128...). | MET3_metrics.json |
| MET4 (mechanism BEYOND metabolism — does PPI-network centrality break the ceiling for the NON-metabolic (FBA-blind) half? NO — a STUDY-BIAS artifact; first-class NEGATIVE that the falsify-first protocol caught) | MET1–MET3's mechanistic win is scoped to METABOLIC targets (FBA is blind to ~half of drug targets: proteases/polymerases/ribosomal — the kind a novel pandemic often presents). MET4 tests the natural extension: is PPI-network topology essentiality (Jeong lethality-centrality / Yu bottlenecks) a mechanistic, non-homology signal for NON-metabolic targets? | E.coli STRING v12 (taxid 511145; 984,760 edges, 4,091 UniProt-mapped — no MET2 locus-tag wall). Non-metabolic subproteome (proteins NOT in the MET2 GEM; n=2734, 88 targets). Per node: conservation (mmseqs to other-org targets) + degree + betweenness centrality on THREE networks — FULL (combined≥700, homology+study laden), EXP (experiments-direct≥400, non-homology but study-biased), COEXP (coexpression-direct≥400, non-homology AND not study-effort-biased). 5-fold-CV nested ΔAUROC. **TWO confound controls (homology + study-bias).** | **INTEGRITY IS THE STORY.** The NAIVE run looked like a strong POSITIVE: on the experimental (non-homology) network, centrality added ΔAUROC **+0.128** beyond conservation (OR 4.4) — 'mechanism extends beyond metabolism!', survived the homology control. STOPPED before committing (Constitution: positives guilty until surviving confounds) and added the STUDY-BIAS control the homology test doesn't remove — drug targets are the MOST-STUDIED proteins, so they accrue experimental PPI edges from research attention (reverse causation), not biology. **Result: the +0.128 is a STUDY-BIAS ARTIFACT.** Study-intensity alone (textmining degree) predicts drug-targethood at AUROC **0.826**; controlling for it, the experimental lift COLLAPSES **+0.128 → −0.004** (gone); the study-bias-robust COEXPRESSION network shows **−0.004** (nothing). **H0/CONFOUNDED: PPI-network centrality is NOT a confound-robust non-homology mechanism for non-metabolic targets.** This SHARPENS the arc: FBA-essentiality is a GENUINE mechanism for metabolic targets (survived all controls, MET1–3); the obvious network-topology extension to the FBA-blind half does NOT survive study-bias → the non-metabolic half remains genuinely hard (no clean zero-data mechanistic signal found yet). Had I committed the first run I'd have shipped a false 'mechanism generalizes' claim — the falsify-first protocol caught it. Also: even the naive positive REQUIRED a measured PPI network (novel pathogen has none). SCOPE: E.coli in-domain; single organism; not wet-lab. Reproduced x2 byte-identical (payload sha256 70e21ec5...). | MET4_metrics.json |
| HIT1 (MOLECULE HALF opens — the novel-chemotype ceiling of zero/low-data ligand-based hit-finding; SOFT ceiling w/ residual signal) | Target-ID is deeply mapped; the north star also needs the MOLECULE half ('produce credible candidates within hours'). Like target-ID, it has an information ceiling the field HIDES: the 2025 audit (arXiv 2507.21404) shows 'zero-shot' VS numbers are inflated by ANALOG LEAKAGE and don't measure novel-chemotype recovery. HIT1 maps that ceiling first (mirroring TID1). | MoleculeACE (30 ChEMBL targets, 48,714 compounds; SMILES + pActivity + cliff + split). SEED = train actives, LIBRARY = test. Two rankers: similarity-transfer (max ECFP4 Tanimoto to seeds) + learned QSAR (RandomForest/ECFP4). Actives = pActivity≥6.5. Novelty split of test actives: analog (nearest-seed Tanimoto≥0.4) vs novel(<0.4). Metrics AUROC/BEDROC/EF overall + novel-vs-inactive. | **INTEGRITY (auto-verdict OVER-READ, caught + corrected PRE-COMMIT):** the raw auto-verdict declared 'H0/ALT learning generalizes beyond analogy — genuine positive' because QSAR beat similarity on novel actives by +0.47 AUROC in 100% of targets. **That is TAUTOLOGICAL:** novel actives are DEFINED as low-similarity-to-seeds, so the similarity ranker scores them low BY CONSTRUCTION (sim novel-AUROC 0.18 ≪ 0.5 — a forced artifact, not 'similarity fails'). Rejected that comparison; re-judged QSAR novel recovery vs RANDOM (0.5) and vs its own overall. **Honest result — SOFT CEILING with RESIDUAL signal:** aggregate potency-ranking works (median overall AUROC sim 0.81 / qsar 0.90) and is ANALOG-DRIVEN (analog-vs-inactive AUROC 0.82); on SCAFFOLD-NOVEL chemotypes a learned QSAR degrades SUBSTANTIALLY 0.90→**0.67** (−0.23) but retains MODEST above-random recovery (>0.55 in 12/15 testable targets). So — UNLIKE the target-ID conservation ceiling (nothing beat conservation for novel) — a learned LIGAND model keeps a partial, noisy novel-chemotype signal; NOT a hard ceiling, NOT 'learning solves novel'. Also caught: BEDROC/EF invalid here (actives are a LARGE fraction → high-prevalence potency task, null BEDROC 0.68 → AUROC is the valid metric). SCOPE/CAVEATS: potency-transfer among measured binders (not active-vs-decoy VS); novel actives RARE (median 5/target, 15/27 testable → low power); novelty defined vs seeds only (permissive); ECFP4/RF (the ceiling, not SOTA); physics/structure floor for novel chemotypes = HIT2. Reproduced x2 byte-identical (payload sha256 eb02d466...). | HIT1_metrics.json |
| FRONT1 (FRONT-HALF chapter — selective mechanistic target discovery; H1 mixed, H2 KEY FINDING: conservation-ranking is THERAPEUTICALLY DANGEROUS, soft-selectivity doesn't fix it → HARD host-nonhomology filter needed) | The biggest capability gap is the FRONT half (mechanism inference + selectivity, both ✗). Built on the one signal that broke the ceiling (MET essentiality), extends the established subtractive-genomics framework (essentiality + metabolic CHOKEPOINT + HOST NON-HOMOLOGY) done fully ZERO-DATA (CarveMe GEM from genome + human-homology) and — the step the field's case-study papers skip — BENCHMARKED vs the conservation baseline with the therapeutic-validity test. | 7 bacteria (reuse MET2 GEMs + cached essentiality); per metabolic-subproteome gene: conservation C (mmseqs to other-org targets), essentiality E (MET2), chokepoint K (COBRApy stoichiometry: unique producer/consumer of a non-currency metabolite), host non-homology S (mmseqs vs human proteome, e<1e-4). Host-toxic (H2 ground truth, independent of ChEMBL) = human homolog is a Hart-CEG2 core-essential gene. H1: 5-fold OOF ΔAUROC [C+E+K+S] vs [C] and vs [C+E]. H2: does the selective composite down-rank host-toxic vs conservation. **Result (H1 MIXED, H2 KEY NEGATIVE→actionable):** H1 — composite beats conservation clearly in E.coli (ΔAUROC +0.095) but only marginally in Mtb (+0.012); the driver is ESSENTIALITY (reconfirms MET) — CHOKEPOINT carries weight only in E.coli (coef 0.37) and is ~zero in Mtb (0.001), soft SELECTIVITY S is weak — so K+S do NOT robustly add beyond conservation+essentiality (ΔAUROC vs C+E: ecoli +0.039, mtb −0.026). **H2 KEY FINDING: CONSERVATION-RANKING IS THERAPEUTICALLY DANGEROUS** — host-toxic targets (human core-essential homologs) are the MOST CONSERVED proteins (mean bitscore host-toxic vs non-toxic: ecoli 123 vs 29, mtb 57 vs 32, Pae 93 vs 39), so the TID1 conservation workhorse actively PROMOTES unsafe targets (host-toxic sit at ~0.52–0.59 percentile). Soft-selectivity (S as a learned feature) does NOT reliably fix it: the composite down-ranks host-toxic in 2/3 orgs (ecoli 0.59→0.46, Pae 0.56→0.52) but UP-ranks them in Mtb (0.52→0.57) where they are also essential/chokepoint — a soft selectivity feature is UNRELIABLE for safety. **CONCLUSION (actionable): front-half therapeutic validity requires a HARD host-non-homology FILTER (excludes ALL host-toxic by construction, since host-toxic ⊂ host-homologous), NOT a soft feature; mechanistic essentiality (MET) remains the recovery driver; chokepoint is organism-specific.** INTEGRITY: auto-verdict was already honest (H1/H2 flags conservative); enriched the write-up to surface the conservation-toxicity finding + hard-filter conclusion (no over-read). SCOPE: metabolic subproteome (FBA-blind non-metabolic half unaddressed, MET4 boundary); 2 reliably-testable bacteria; ChEMBL selection-bias caveat on H1 (targets pre-selected for host-nonhomologous druggable essentials); chokepoint topological heuristic; CEG2 cell-line-derived; not wet-lab. Reproduced x2 byte-identical (payload sha256 9004b17c...). | FRONT1_metrics.json |
| E2E2 (CORRECTED zero-data front-half pipeline — the safety/recall tradeoff: an HONEST TENSION, both naive and hard-filter are wrong) | E2E1 ranked by conservation; FRONT1 showed that's therapeutically dangerous and the fix is a HARD host-nonhomology filter. E2E2 builds the corrected pipeline (mechanistic essentiality + hard host-nonhomology filter + calibrated abstention) and answers the decision-relevant question: making the shortlist SAFE removes host-toxic targets — at what recall cost? | Mtb + E.coli (reuse MET2 GEMs + FRONT1 signals). NAIVE = rank all metabolic genes by conservation C. CORRECTED = hard-filter host-homologous (S=0) → rank remainder by UNSUPERVISED mechanistic composite z(C)+z(E)+z(K) → abstain on no-homolog genes. Shortlist = top-k (k=#targets). Host-toxic = human homolog is Hart-CEG2 core-essential. | **Result — HONEST TENSION (a genuinely informative negative-shaped finding):** SAFETY is real — the corrected top-k contains 0 host-toxic targets vs the naive conservation shortlist's 5 (Mtb) / 9 (E.coli) host-toxic (human-essential-homolog) targets. TOP-K RECALL looks nearly preserved (corrected/naive 0.81 Mtb, 1.0 E.coli; precision@k mtb 0.239→0.194, ecoli 0.277→0.277). **BUT the real cost is LARGE + STRUCTURAL: the blunt sequence-level hard filter PERMANENTLY EXCLUDES 52% (Mtb) / 35% (E.coli) of ALL known drug targets — those WITH a human homolog — from the searchable space; the pipeline can NEVER find them.** Top-k recall only looks preserved because base recall is low and surviving host-nonhomologous targets rank well. Many host-homologous targets ARE drugged SELECTIVELY in reality (binding-site differences a sequence filter can't see). **NET: naive-conservation is UNSAFE (promotes host-toxic, which are the MOST conserved); the hard filter is SAFE but OVER-EXCLUDES a large fraction of real targets; genuinely correct selectivity needs BINDING-SITE-level pathogen-vs-host difference reasoning (structure, not sequence homology) — which sequence transfer cannot provide (another INFORMATION-CEILING instance).** Abstention IS calibrated (no-homolog genes are target-poor). INTEGRITY: auto-verdict claimed 'recall cost is LOW' — CONTRADICTED by its own 35-52% exclusion numbers; corrected PRE-COMMIT to the honest tension (top-k recall preserved ≠ targets not excluded). SCOPE: metabolic subproteome; 2 bacteria; unsupervised zero-label composite; ChEMBL selection-bias likely UNDER-estimates exclusion for a truly novel pathogen; molecule-half weak (C1/HIT2); not wet-lab. Reproduced x2 byte-identical (payload sha256 10b1a09e...). | E2E2_metrics.json |
| HIT2 (molecule-half PHYSICS floor — H0: structure-only docking gives NO usable potency-ranking signal; completes the molecule-half map) | HIT1 showed ligand hit-finding is analog-bound. The one signal NOT dependent on chemical analogy is structure-based physics (docking). HIT2 asks: does docking recover the actives ligand-transfer can't, on the SAME compounds? | Thrombin (CHEMBL204); receptor 1OYT (cognate inhibitor FSN redocked −11.21 kcal/mol = pocket validated); 553 MoleculeACE test compounds docked with AutoDock Vina 1.2.7 (seed 42, exhaustiveness 16, ~13.8h, deterministic-cached; 24 failed docks). Physics=−Vina vs similarity vs QSAR; overall + novelty split (same as HIT1). | **Result (H0 — no usable physics floor for potency-ranking):** thrombin docking ranks potency among binders NO BETTER THAN RANDOM (overall AUROC **0.43**, slightly anti-predictive; analog 0.43, novel 0.52). Adding docking to the ligand signal HURTS (consensus 0.70 < ligand-similarity ~0.84). **Honest scope (crucial):** MoleculeACE = a CONGENERIC BINDER SERIES, so HIT2 tests FINE POTENCY-RANKING (docking's hardest case), DISTINCT from coarse active-vs-decoy hit-finding where C1 saw a weak-but-real signal (Mpro AUROC 0.63); the novel-chemotype physics test is UNDERPOWERED (only 5 novel actives — thrombin data is analog-heavy) so the 'physics beats similarity on novel' flag is both the HIT1 tautology (novel:=low-similarity) AND noise. INTEGRITY: verdict enriched pre-commit to foreground these scope limits (not over-read to a general 'docking fails'). **NET molecule-half map (C1+HIT1+HIT2): ligand methods are analog-bound (HIT1); docking is weak-but-real for active-vs-decoy separation (C1) but USELESS for within-series potency-ranking (HIT2); no method has strong traction on genuinely novel chemotypes — the information ceiling, same as target-ID.** SCOPE: single favourable target, Vina, potency data (not decoys), 5 novel actives, not wet-lab. Reproduced x2 byte-identical (payload sha256 813f2034...). | HIT2_metrics.json |
| FRONT2 (structural selectivity — H0: structure does NOT rescue the host-homologous targets E2E2 over-excludes; the information ceiling extends from sequence to STRUCTURAL selectivity) | E2E2 showed the sequence-level host-nonhomology filter over-excludes 35-52% of real targets (host-homologous, many drugged selectively via binding-site differences). FRONT2 tests whether STRUCTURE recovers what sequence loses. | Host-homologous pathogen metabolic genes (Mtb + E.coli): all host-homologous known targets (35 Mtb, 33 E.coli) + seeded 100 non-targets/org. Per gene: pathogen fpocket max Druggability Score (reuse TID2 cache else fetch AlphaFold v6 + fpocket) + best human-homolog fpocket druggability (fetch AF + fpocket; validated thrombin human P00734=0.617). Signals: pathogen druggability + selectivity difference (path−human). AUROC vs known-target, permutation null. | **Result (H0 — clean negative):** among host-homologous genes, pathogen pocket druggability does NOT distinguish known targets from non-targets (pooled AUROC **0.511**, perm p 0.41; mtb 0.465, ecoli 0.551) and the pathogen-vs-host druggability DIFFERENCE adds nothing (AUROC 0.535; mtb 0.557, ecoli 0.508). **So zero-data structural druggability CANNOT cheaply rescue the 35-52% host-homologous targets the sequence filter over-excludes — the information ceiling extends from SEQUENCE to STRUCTURAL selectivity** (consistent with TID2, where fpocket pocket druggability ≈ conservation, weak). Distinguishing which host-homologous targets are SELECTIVELY druggable needs more than an apo predicted pocket (real ligand / induced-fit / experimental data). No over-read (AUROC≈random, perm-NS → H0 honest + scoped, not a universal 'structure is useless' claim). **SELECTIVITY SUB-ARC COMPLETE (FRONT1→E2E2→FRONT2): conservation-ranking is unsafe (promotes host-toxic); the sequence hard-filter is safe but over-excludes; structure can't rescue the over-excluded zero-data → true selectivity is resource-gated (needs experimental data).** SCOPE: fpocket heuristic, AF apo structures, metabolic subproteome, 2 bacteria, sampled non-targets, ChEMBL selection-bias; not wet-lab. Reproduced x2 byte-identical (payload sha256 c1824589...). | FRONT2_metrics.json |
| SUBSTRATE1 (the EXTENSIBLE EVIDENCE SUBSTRATE — the vision's "any disease → a query" differentiator, shipped; composition+governance layer that operationalizes the whole arc) | The capability map's biggest ✗ was the extensible, provenance-tiered "living substrate" (charter law U2: no disease-specific code; disease-awareness via config; continuous-absorption guardrail). Now justified: the signals are validated, so build the layer that composes them safely + extensibly. | `src/intercepta/substrate.py` (pure-Python, data-free-testable core: ProvenanceTier [QUARANTINED<OWN_HYPOTHESIS<OWN_SINGLE<OWN_REPRODUCED<EXTERNAL_VALIDATED], EvidenceRecord/Provider/Store, TargetEngine) + `substrate_providers.py` (real adapters: Conservation [mmseqs], CacheRank [FBA-essentiality/chokepoint], HostToxicSafety [CEG2+human], NoHomologAbstain). 8 data-free unit tests (tests/test_substrate.py) + SUBSTRATE1 integration demo. | **Result (built + validated + reproduced):** the substrate runs 'genome → a query' end-to-end on E.coli+Mtb with a DISEASE-AGNOSTIC core (U2 — only the query changes), composing conservation + FBA-essentiality + chokepoint under PROVENANCE-TIERED governance and baking in every arc correction: **HARD safety filter EXCLUDES host-toxic by construction (0 in every shortlist — reproduces E2E2's safety); host-homologous survivors FLAGGED `needs_experimental_selectivity` (608 E.coli/463 Mtb — E2E2/FRONT2 honesty); continuous-absorption GUARDRAIL (self-generated evidence auto-QUARANTINED until reproduced/promoted — the anti-self-deception mechanism); honest abstention (fires in conservation-only regime; mechanism covers all metabolic genes here).** **And it BEATS the naive baseline on recovery: safe shortlist recovers 33/94 (E.coli) + 17/67 (Mtb) known targets — MORE than E2E2's naive-conservation 26/13 — because it properly composes mechanism with conservation+chokepoint** (top hits real druggables AcrB/IspF/MurG, high-confidence 3-signal). INTEGRITY: caught + fixed an over-aggressive abstention (conservation-only NoHomologAbstain wrongly discarded mechanism-only targets — contradicts MET; removed from the mechanism-equipped demo, kept for conservation-only use) before commit. Full suite 41/41 tests pass (no regressions). SCOPE: composition+governance layer (does not itself validate biology — each provider carries its own LEDGER tier); metabolic subproteome demo; outputs are confidence-tiered hypotheses with full provenance, NOT validated targets; not wet-lab. Reproduced x2 byte-identical (payload sha256 f9a76e56...). | SUBSTRATE1_metrics.json + tests/test_substrate.py |
| SUBSTRATE2 (the substrate is ENTITY-AGNOSTIC — the SAME core ranks MOLECULES, not just proteins; spans the whole pipeline) | Extends the substrate to prove its central architectural claim (charter U2 / "any disease → a query"): the disease-agnostic core should compose ANY entity's evidence, not only proteins. | Added molecule (back-half) providers to `substrate_providers.py`: QEDProvider (drug-likeness, RANK), SAscoreProvider (synthesizability, RANK direction=-1), StructuralAlertSafetyProvider (PAINS structural alert, SAFETY_FILTER) — free RDKit descriptors, no training. SUBSTRATE2 demo runs the SAME `TargetEngine` core over 2,754 real ChEMBL molecules (MoleculeACE). | **Result (built + validated + reproduced):** the SAME disease-agnostic core that ranked PROTEINS (SUBSTRATE1) ranks 2,754 candidate MOLECULES purely by swapping providers — EXCLUDES 112 (4.1%) PAINS structural-alert liabilities by construction (0 in shortlist — the molecule-half analogue of the host-toxic filter), ranks the rest by composed developability (top hits high-QED 0.88 + low-SAscore 1.47 = drug-like + easily synthesizable), tiers confidence identically. **Proves the "any disease → a query" architecture is ENTITY-AGNOSTIC and spans the whole discovery pipeline (front-half proteins AND back-half molecules through ONE governance layer)** — the extensible living net. +1 data-free unit test (entity-agnostic molecule ranking + PAINS exclusion); full suite 42/42 pass. SCOPE: free RDKit descriptors (developability/liability composition), NOT activity or a validated drug; the binding/potency stage stays weak (C1/HIT2); outputs are hypotheses; not wet-lab. Reproduced x2 byte-identical (payload sha256 68bf3597...). | SUBSTRATE2_metrics.json + tests/test_substrate.py |
| SUBSTRATE3 (CONTINUOUS ABSORPTION + anti-self-deception guardrail — the "living net" principle 6, demonstrated on real data) | The last un-demonstrated core vision principle: the system is LIVING (absorbs external + its OWN findings), but self-generated evidence must never masquerade as ground truth (or the self-improving loop becomes a self-deception loop). Built into the substrate (provenance tiers + quarantine/promote, unit-tested); SUBSTRATE3 demonstrates it in action. | Real E. coli substrate. The net absorbs strong SELF-derived predictions (OWN_HYPOTHESIS) about a real target + a non-target; separately a genuinely INDEPENDENT experimental result arrives (EXTERNAL_VALIDATED). Compare the demo target's decision across: baseline / self-derived-absorbed / external-absorbed. | **Result (demonstrated + reproduced):** (1) GUARDRAIL HOLDS — self-derived evidence is QUARANTINED and INERT: absorbing a strong self-belief about target P0AEB2 leaves its score UNCHANGED (base 0.686 = self-derived 0.686) → the loop CANNOT hallucinate itself into confidence (no self-reinforcement). (2) A self-belief about a NON-target (Q47146) NEVER reaches the shortlist. (3) NET GROWS ONLY ON EXTERNAL evidence — when an INDEPENDENT experimental result arrives (EXTERNAL_VALIDATED, not self-derived), P0AEB2's score rises 0.686→1.665. INTEGRITY: caught + fixed a conceptual error mid-build — the first version PROMOTED the self-loop's OWN output to EXTERNAL (which IS the self-deception the guardrail prevents, and z-scored to 0 as a single-entity signal); corrected so growth comes only from genuinely external evidence. **This is SIL1/SIL2 (self-improvement is real + safe ONLY when confidence-gated) enforced STRUCTURALLY by the substrate — the living, self-absorbing net that grows on validated knowledge and quarantines its own guesses.** SCOPE: composition/governance demonstration; the external result is simulated to exercise the guardrail; not wet-lab. Reproduced x2 byte-identical (payload sha256 e818e31f...). | SUBSTRATE3_metrics.json |
| SUBSTRATE4 (PANDEMIC STRESS TEST — the substrate on SARS-CoV-2, the north-star's motivating case; a genuinely different disease class, honest degradation demonstrated) | The north-star stress test: a novel pandemic virus, proteome only, zero data — can the "any disease → a query" engine respond? A virus has NO metabolic model, so the mechanism signals (FBA-essentiality/chokepoint) that broke the ceiling for bacteria DO NOT EXIST — the substrate must degrade honestly. | SARS-CoV-2 proteome (17 proteins; tid1) + known targets {Spike P0DTC2, Nucleoprotein P0DTC9, Replicase P0DTD1}. Two configs: (A) conservation reference = a RELATED coronavirus's target homologs (SARS-CoV-1 Spike/N/Replicase P59594/P59595/P0C6X7 — the 2003 "prior knowledge"); (B) reference = the BACTERIAL panel only (isolated). Providers: conservation + host-nonhomology safety + abstention; NO mechanism (viral). | **Result (all 3 honest behaviours demonstrated + reproduced):** (A) HOMOLOGY-ANCHORED recovers **3/3** known targets (Spike/N/Replicase) — the real mechanism by which SARS-CoV-2 targets were prioritized in weeks (VISION doc: ~96% Mpro/RdRp identity to SARS-CoV-1) — but at **'moderate'** confidence, NOT 'high': with no metabolic model there is only ONE signal (conservation), so the substrate **AUTO-DEGRADES confidence by construction** (vs 'high' 3-signal bacterial targets). (B) With only a bacterial reference the virus is phylogenetically ISOLATED (TID3), so the substrate **ABSTAINS on 17/17 proteins, recovers 0 — it KNOWS it is out of its depth** rather than being confidently wrong (the honest anti-silent-failure behaviour). **This is the north-star 'any disease → a query' behaviour on the ACTUAL pandemic case: homology-anchored where a relative exists, honestly abstaining where none does, never over-confident without mechanism — the vision's core claim demonstrated on a genuinely different disease class (virus, not bacterium).** SCOPE: 17-protein viral proteome; targets at polyprotein level; conservation-only (no viral mechanism signal exists — an honest data-class limit, not a bug); reference A is a closely-related coronavirus (the favourable homology case; the isolated case is B); demonstration on validated signals; not wet-lab. Reproduced x2 byte-identical (payload sha256 871eccaf...). | SUBSTRATE4_metrics.json |
| SUBSTRATE5 (the substrate on a HUMAN DISEASE — 'any disease' spans infectious AND non-infectious; ARCHITECTURE positive + an HONEST study-bias caveat) | The fullest vision is 'ANY disease' — everything prior was pathogens. SUBSTRATE5 tests whether the SAME entity-agnostic core does target-ID for HUMAN (non-infectious) disease, unifying the original cancer arc (B34/DepMap) with the pathogen arc under one governance layer. Zero-cost (existing Open Targets + DepMap data). | 3 cancers (NSCLC/breast/melanoma; Open Targets 40-disease panel). Providers: OpenTargetsProvider (genetic_association + somatic_mutation + affected_pathway → RANK, EXTERNAL tier; B34) + SetSafetyProvider (DepMap PAN-ESSENTIAL genes = common-essential across ≥90% of 1096 cell lines = toxic to inhibit → SAFETY_FILTER, the human analog of host-toxic). Ground truth = OT clinical>0 (clinic-reached). | **Result — POSITIVE (architecture) + HONEST CAVEAT (study bias):** the SAME core runs human-disease target-ID across 3 cancers, composing curated evidence under provenance governance and EXCLUDING pan-essential (toxic) genes by construction (1020 pan-essential genes; **0 in every shortlist** — the human host-toxic analog works). **So 'any disease → a query' spans INFECTIOUS (pathogens, SUBSTRATE1-4) AND NON-INFECTIOUS (cancer) through ONE governance layer — the architecture generalizes to the fullest 'ANY disease' claim.** INTEGRITY: auto-verdict first claimed 'beats popularity, reproducing B34' — CAUGHT as FALSE (data shows the opposite): on RAW top-k recovery the genetic composite does NOT beat a study-popularity (literature) baseline (0/3; composite 38/9/18 vs popularity 67/28/30) because clinic-reached targets ARE the most-studied → literature trivially recovers them — **the SAME popularity/study-bias confound that killed MET4's PPI signal, now recurring for human-disease target evidence.** **DOUBLE-CORRECTION (falsify-first applied to my own EXPLANATION):** I first wrote the low recovery off as a top-k metric artifact fixable by a popularity-control provider (as B34 did) — then VERIFIED on the FAIR, prevalence-independent AUROC metric, which REFUTED that: within a single disease the genetic composite AUROC (0.36/0.31/0.53) is NEAR/BELOW RANDOM and loses to popularity (0.59/0.54/0.73) in 3/3 — the evidence is genuinely WEAK within-disease, not just mis-metriced. B34's positive (genetic beats popularity) was a CROSS-disease TRAINED model (leave-disease-out); it does NOT transfer to the substrate's SINGLE-disease query. Also verified the pan-essential filter is NOT the cause (recovery 44 vs 48 with/without it). **HONEST 'any disease' BOUND: the governance ARCHITECTURE is universal, but the QUALITY of target-ID is disease-class-specific — STRONG for pathogens (real mechanism/essentiality signal) but WEAK for human single-disease queries (popularity-confounded, near-random evidence — a real ceiling consistent with the original arc's human-disease nulls B10/B23).** Full suite 43/43 pass. SCOPE: composition/governance on curated evidence; human drug-response has hard ceilings (B10/B23); hypotheses not validated targets; not clinical. Reproduced x2 byte-identical (corrected run, payload sha256 12444e43...). | SUBSTRATE5_metrics.json |
| PANBACT (pan-bacterial NOVEL-target catalog — the method reconstructs the CANONICAL antibacterial target landscape from 7 genomes + zero drug data) | Generalize the locked E. coli predictions (EXPVAL) across the whole panel: what novel safe targets does the substrate predict for EACH of the 7 bacteria, and which are broad-spectrum? Zero new data (reuses caches). | Per bacterium: FBA-essential (MET2) + metabolic chokepoint (FRONT1) + host-non-homologous (mmseqs vs human, e<1e-4) + NOT a known ChEMBL target. Broad-spectrum = shared gene name across ≥3 bacteria. | **Result (reproduced x2): 85 novel safe target predictions across 7 pathogens** (ecoli 7, mtb 11, Pae 19, Bsub 15, Hpylori 13, Salmonella 14, Efaecalis 6). **The broad-spectrum predictions (≥3 bacteria) are EXACTLY the canonical antibacterial target pathways:** cell-wall/peptidoglycan **murB(5), murG(5), murF(4), mraY(3)** (the β-lactam/vancomycin target space — validated-essential, largely undrugged), isoprenoid/MEP **ispG(4), ispE(4), dxr(4)**, CoA **coaD(4)**, + menaquinone/folate/riboflavin/thiamine per-organism. **From 7 genomes + ZERO drug data the method independently reconstructs the established antibacterial target landscape — strong multi-pathogen corroboration the mechanism biology is real** (complements EXPVAL literature check + BROADSPEC convergence). SCOPE: FBA-predicted essentiality (experimental check = VALIDATE_essentiality Tier 0); chokepoint heuristic; sequence host non-homology; broad-spectrum by shared gene name (coarse ortholog proxy); hypotheses, not validated targets; not wet-lab. Reproduced x2 byte-identical (payload sha256 5c6e646c...). | PANBACT_metrics.json |
| DRUGGABLE (complete the target-quality profile — fpocket druggability of the 85 pan-bacterial predictions → fully-profiled prioritized targets) | A target needs a druggable POCKET; the predictions were essential+chokepoint+safe+broad-spectrum but druggability was unassessed. Add it (AlphaFold v6 + fpocket, reuse FRONT2 path; zero-cost). | 85 predictions (PANBACT) → fetch AF structure + fpocket max Druggability Score per gene (max across orthologs); druggable ≥ 0.5. | **Result (reproduced x2): the fully-profiled TOP targets (broad-spectrum ≥3/7 AND druggable) are murB (breadth 5/7, druggability 0.95), murG (5/7, 0.89), dxr (4/7, 0.94), murF (4/7, 0.92), ispE (4/7, 0.67), ispG (4/7, 0.61), menC (3/7, 0.93), mtnN (3/7, 0.82), mraY (3/7, 0.79)** — every one a canonical antibacterial target class (cell-wall/peptidoglycan mur*/mraY, isoprenoid/MEP dxr/isp*, menaquinone menC). These satisfy ALL zero-data target criteria simultaneously — FBA-essential + metabolic chokepoint + host-non-homologous (safe) + broad-spectrum + druggable pocket — the strongest, most complete predictions for experimental validation (murB the standout: druggability 0.95, essential across 5/7 bacteria, host-absent, undrugged). Completes the prediction-profiling line EXPVAL→BROADSPEC→PANBACT→DRUGGABLE. SCOPE: fpocket heuristic on AlphaFold APO structures (TID2 found druggability modest as a discriminator; used here as a per-target pocket-quality profile, not a ranking claim beyond conservation); FBA essentiality (experimental check = VALIDATE Tier 0); hypotheses, not validated targets; not wet-lab. Reproduced x2 byte-identical (payload sha256 3692a372...). | DRUGGABLE_metrics.json |


## FALSIFIED (removed from the working hypothesis, with the killing test)
- **Therapy-SELECTION coordinate system** — 0/16 subtype-adjusted axis×arm interactions survive BH in I-SPY2
  n=990 (selection_metrics.json). Prognostic, not selective.
- **Therapy-class specificity (axis→matched therapy)** — R_immune predicts pCR broadly; Pembro-specific
  interaction permutation p=0.33 (gse194040_metrics.json).
- **Novel coordinate beyond Ki67+TILs** — composites add over single genes but r(R_immune,CD8A)=0.78,
  r(R_prolif,MKI67)=0.43 → same two known axes (compression_metrics.json).
- **PI3K mechanistic specificity** — flips on GDSC1 external (m5b_external_gdsc1_metrics.json).
- **Scout-2 "de novo generative design"** — corrected to scaffold-hopping (INTERCEPTA VISION_AUDIT).

## REFINED by the genome-wide screen (B5) — earlier pairwise claims tightened
- **V4 NPM1→Cabozantinib** and **V6 DNMT3A→Dasatinib** do NOT independently survive genome-wide BH-FDR when
  co-adjusted for FLT3-ITD. They are real but MODEST pairwise effects; cabozantinib sensitivity is largely the
  co-occurring **FLT3-ITD** (the genome-wide-robust cabo marker). **V5 NRAS→MEK is CONFIRMED genome-wide.**
  The engine's marker table was updated accordingly (cabo→FLT3-ITD; DNMT3A→dasatinib dropped).

## NOT TESTABLE ON CURRENT DATA (Constitution outcome 3 — with the exact missing data)
- **RNA-velocity "time machine" (pre-resistance detection)** — velocity outputs are pseudotime/magnitude only;
  no per-cell drug-response/resistance ground truth in any available scRNA. Needs paired longitudinal scRNA
  (baseline + relapse, same patients) with matched outcome, ideally lineage-traced. Architecturally novel,
  empirically untested — do NOT claim it works. (RNA_VELOCITY_FEASIBILITY.md)
- **Therapy-selective axis in non-ER / metastatic settings** — needs controlled-access RCTs with
  treatment×biomarker design (dbGaP/EGA) — a human gate.

## HUMAN CLINICAL VALIDATION — NULL (decisive, first-class; B10)
- **TCGA human drug-response (12 drugs, 1079 patients, public):** the cell-line transfer's raw association with
  clinical response (pooled AUROC 0.539, p=0.036) is **entirely CANCER-TYPE CONFOUNDING** — within-cancer
  stratified AUROC = **0.504 (p=0.43), NULL**. Proliferation-only AUROC 0.444. **Conclusion: the engine does NOT
  predict human clinical drug response at the drug level once cancer type is controlled.** It is a cell-line /
  ex-vivo tool (V1 transfer, V9 BeatAML drug-specificity), NOT a validated human clinical predictor. This bounds
  the fullest vision honestly: clinical-grade patient prediction is NOT achieved on available human data. | B10_metrics.json |
- **Functional-layer clinical-outcome — NULL (B17, decisive, first-class):** the strong *ex-vivo* signal (V19,
  inferred-FLT3-dependency→FLT3i ex-vivo response, p=1e-15) does **NOT** translate to a survival benefit in
  retrospective BeatAML. Cox interaction `dep_score × FLT3i-treated` on overall survival (n=644 patients, 395
  deaths, 106 FLT3i-treated): **HR=0.893, p=0.56 — no interaction.** Within FLT3i-treated dep→OS HR=0.916
  (p=0.50); within untreated HR=0.974 (p=0.65). Even FLT3-ITD is not significantly prognostic here (logHR +0.13,
  p=0.33), reflecting treatment heterogeneity and no treatment timing. **Interpretation (pre-fixed):** BeatAML has
  no treatment dates / no first-line FLT3i, so immortal-time + confounding-by-indication bias *toward* a spurious
  positive; the null is therefore the cleanly interpretable, robust outcome. **This bounds V19 exactly: it is an
  ex-vivo functional lead, and the clinical (survival) endpoint is NOT establishable on retrospective data —
  it requires prospective Track-1.** Reproduced ×2 (identical). | B17_metrics.json |

## EXTERNAL REPLICATION — FAILED (decisive, first-class; B20) — the functional-layer result is BeatAML-specific
- **V19/V20 do NOT replicate in an independent AML cohort.** Tested in the FIMM/Malani cohort (Zenodo 7370747,
  CC-BY 4.0; Malani et al. Cancer Discovery 2022) — a different institution (Helsinki) and assay (DSRT drug
  sensitivity score, DSS) than BeatAML (OHSU probit AUC); 163 AML patients with matched RNA + 7 FLT3 inhibitors +
  venetoclax + FLT3/NPM1 mutations. Pre-registered (B20).
  - **R1 (V19 core):** inferred-FLT3-dependency → FLT3-inhibitor sensitivity, proliferation-adjusted pooled across
    7 FLT3i: **ρ=+0.047, one-sided p=0.082 — NULL** (per-drug inconsistent: sorafenib −0.13, quizartinib +0.11).
  - **R2 (V20 specificity):** diagonal +0.053 vs off-diagonal +0.025, permutation **p=0.13 — NULL** (venetoclax is
    weakly BCL2-specific, +0.085 vs FLT3 −0.021, the one directionally-consistent fragment).
  - **R3 (beyond-mutation):** meta β=−0.02 (p=0.92); within FLT3-mutation-negative ρ=−0.003 — **NULL**.
  - **This is a fair test, not an artifact (verified):** 96.5% engine-feature-gene overlap; inferred-FLT3-dep tracks
    FLT3 expression (ρ=+0.38) so the layer functions; and crucially the KNOWN biomarker replicates strongly —
    FLT3-mutation predicts FLT3i DSS in FIMM for all 5 drugs (MWU p=1e-5…1e-3). So FIMM captures FLT3i biology; what
    fails is specifically OUR inferred-dependency layer's *added value beyond mutation*.
  - **Conclusion (truth over ambition):** the V19/V20 functional-inference results are **BeatAML-specific and do not
    generalize** to an independent AML cohort. The known FLT3-mutation→FLT3i relationship generalizes; our
    inferred-layer refinement does not. V19/V20 remain internally valid (reproduced ×2 in BeatAML, cross-lineage
    robust B19) but are **downgraded from "translational lead" to "single-cohort finding, externally falsified."**
    Reproduced ×2 (identical). Data sha256 in metrics. | B20_metrics.json |
- **B21 (post-hoc closure): no narrow cross-cohort slice survives either.** Tested whether the signal holds for
  FLT3-SELECTIVE inhibitors (a-priori pharmacology: quizartinib, crenolanib, gilteritinib) consistently across
  BeatAML + FIMM. Per-drug prolif-adjusted effects are INCONSISTENT across cohorts: sorafenib (the STRONGEST in
  BeatAML, +0.25) FLIPS to −0.13 in FIMM. The selectivity hypothesis FAILS (H2): BeatAML selective +0.17 < promiscuous
  +0.25 (gap −0.08, perm p=0.83); FIMM gap +0.06 but p=0.17 — not consistent. Honest footnote (does NOT meet the
  pre-registered bar): the two selective drugs quizartinib (+0.15/+0.11) and crenolanib (+0.22/+0.07) are weakly
  positive in both cohorts — hypothesis-generating at most, post-hoc, underpowered (≤3 shared drugs). **Verdict:
  DOOR CLOSED — B20's non-replication is confirmed at the per-drug level; the functional-inference computational
  avenue is exhausted; a generalizable functional layer needs perturbation data measured IN patients (Track-1).**
  Reproduced ×2. | B21_metrics.json |

## UNDERPOWERED / INCONCLUSIVE (honest, first-class)
- **B11 novel-marker cross-system replication:** 14 pre-declared NOVEL (non-textbook) BeatAML mutation->drug
  associations tested in pan-cancer DepMap cell lines (mutations + PRISM/GDSC response). **0/13 replicate**
  (BH<0.05 + same direction) -> AML-specific or single-cohort artifacts, NOT generalizable discoveries. | B11_metrics.json |
- **B8 PDXE mechanism-marker test:** established solid-tumor markers on PDXE. PIK3CA→alpelisib shows the correct
  SENSITIZING direction (β=−23.4, nominal p=0.043) and engine>parts (CV 0.165>0.095>0.013), consistent with known
  biology (alpelisib is FDA-approved for PIK3CA-mut) — but only 15 functional-mutant PDX, so it does NOT survive
  multiple-testing correction (BHq=0.087). buparlisib null; RAS→trametinib too few mutant models. PDXE is
  underpowered for marker tests (unlike the transfer test B7). Not a pipeline failure; not a validation. | B8_metrics.json |

## Probability estimates (subjective, evidence-conditioned)
- P(novel therapy-selection coordinate system correct): **<5%** (falsified at power).
- P(prolif+TILs prognostic, known biology, no selection): **~85–90%**.
- P(a genuinely novel proliferation-independent selective axis exists & findable in better data): **~10–20%**.
- P(functional-inference layer [V19/V20] adds real value beyond FLT3-mutation, generalizably): **DOWNGRADED to ~10–15%** after B20 external-replication failure (was ~60% on BeatAML-internal evidence). The inferred layer's added value beyond the standard biomarker did not survive independent replication; what generalizes is the known FLT3-mutation→FLT3i biology, not our refinement. A genuine generalizable functional layer likely needs functional (perturbation) data measured IN the target patients, not inferred from a pan-cancer cell-line map (→ Track-1).
