# INTERCEPTA — Python Dependencies
# Install: pip install -r requirements.txt

# Core (required for engine + KAALCURA)
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3

# Visualization (optional but recommended)
matplotlib>=3.7

# GDSC data loading
openpyxl>=3.1

# scRNA-seq / RNA velocity (Phase 2)
# scanpy>=1.10
# scvelo>=0.3
# cellrank>=2.0
# anndata>=0.10

# Network analysis (pipeline)
networkx>=3.1
