# docs/

- **`vision/`** — the reconstructed, evidence-anchored north-star (`INTERCEPTA_TRUE_VISION_AND_PLAN.md`) + research charter. **Current & authoritative.**
- **`audits/`** — self-audit scorecards + the ODE limitations doc (honest self-corrections).
- **`aspirational_original/`** — QUARANTINED founding vision docs; contain claims later falsified (banner-tagged). History only.
- **`project/`** — the full working/historical document corpus carried over from the project (specs, memos, closures, charters, round2/3 docs, references). Preserved for completeness.
- **`status/`** — top-level working status/notes files (`INTERCEPTA_STATUS`, `MASTER_FIXES`, `CLAUDE.md`, etc.).

> ⚠️ **Authoritative record:** `../LEDGER.md` (verified/falsified/untested), `../VISION.md`, and this folder's
> `vision/` + `audits/` are the source of truth. Documents under `project/` and `status/` are the working
> corpus and **may contain superseded or overstated claims** — where they conflict with `LEDGER.md`, the
> ledger wins. Nothing here is a validated clinical result.
